# 更新日志

版本号规则：`major.minor.patch`，功能波升 minor，修补升 patch。
安装包命名：`DeskMinis-<版本>-Setup.exe`（安装版）/ `DeskMinis-<版本>-win-x64-portable.exe`（便携版）。

## 0.3.0 — 待发布

**首个公开版本**，发布在公开仓库 [lincheuk/deskminis-releases](https://github.com/lincheuk/deskminis-releases/releases)。
0.2.0 没有公开发布过，它的内容（见下方 0.2.0 一节）一并随本版首发。

界面第二次重做 + Office 文档能力 + 一批换壳时漏掉的能力补齐；发版前再做一轮止血——
会伤数据、会越权的问题，和静默失效、界面说假话的问题，都在这一版修掉（见「数据安全」「失效与界面诚实」两节）。

### 界面（第二次重做）

0.2.0 那版蓝白界面**被推倒重来了**——不是继续改，是新建一套。原因是它始终停在
「照着参考图改造」的层次：字号挤在 11–17px 没有层次、全局根本没有 line-height、
圆角偏紧、主色还错把参考项目的一档灰阶当成了品牌色，界面因此整体发闷。

- **新设计系统**：颜色 / 字号行高 / 圆角 / 间距 / 阴影五套令牌各自独立命名空间，
  字号与行高**成对定义**（缺行高是上一版「质感不对」的头号成因，比圆角更致命）。
- **三栏工作台**：agent 做出来的东西占主舞台，对话退到左边一条。产出物预览支持
  源码 / 渲染 / 分栏对照三态，带行号槽；打开过的产出物各占一个标签页，来回对照不用重找。
- **随包字体**：内置思源黑体（SIL OFL 1.1），字体栈按 Windows 重排——上一版照搬了
  macOS 优先的字体栈，中文在 Windows 上回落到雅黑，反而比更早的版本更糟。
- **设置成为独立页面**：模型 / 权限 / 技能 / MCP / 网络搜索 / 外观 / 关于七节。
  主题三态（跟随系统 / 浅 / 深）挪进「外观」。

### 新能力

- **Office 文档**：读和生成 `.docx` / `.xlsx` / `.pptx`。agent 有 `office_read` /
  `office_write` 两个工具，预览区直接渲染内容——Word 一张纸、Excel 带行号列名的网格
  （多表出标签页）、PPT 一页一张 16:9 卡片。
  **诚实边界**：这是**内容预览**不是版式还原，文字、表格、大纲都在，但字体、精确排版、
  图片位置、动画不还原，要看最终版式请用系统 Office 打开；旧版 `.doc/.xls/.ppt` 与 ODF
  是另一套二进制格式，明确不支持（不给假希望）。零新依赖实现。
- **扩展市场、任务面板**进入新界面：右栏新增「任务」tab，能看上下文水位与降级 / 压缩 / 卸载 / 待批准四态——
  输出突然变差多半是降级到备选模型了，上下文突然「忘事」是触发了压缩，这些以前只在日志里。
  底部的终端抽屉也随新界面保留：它在本会话工作区里另起一个 PowerShell，与 agent 的 shell 不共享 cd 和环境变量。
- **会话搜索**（按标题；消息正文还没有索引，界面上如实说明了）。
- **模型组降级有了界面**（后端早就建成，一直点不到）：设置 → 模型 → 模型组，把几个模型排成顺序；
  会话菜单和助手的「模型」都能选组。限流、密钥失效、请求被拒会立刻换下一个；网络中断和服务端错误
  先在原模型上重试约一分钟再换。换成功后这个会话会改绑到接手的模型、不再回到组里——编辑器里写明了，
  会话菜单和模型胶囊也会跟着显示改绑后的模型。

### 数据安全

- **`file_edit` 不再静默改坏文件**。以前：替换文本里的 `$&`、`$$` 这类写法被当成替换模式展开；CRLF 文件配 LF 写的
  old_string 报「找不到」，LF 的替换文本写进 CRLF 文件留下混合行尾；重叠出现（在 `aaa` 里改 `aa`）被当成唯一、静默改了第一处；
  空文件配空 old_string 会往文件头插内容；GBK / UTF-16 文件被按 UTF-8 读写，坏字节全变成 �。
  现在只替换命中的那一段、保持原行尾与 BOM；非 UTF-8 文件、重叠出现、空的 old_string 一律拒绝，不写盘。
- **技能包解压有上限**：2000 个文件、总计 64MB、单个文件 32MB——解压之前按条目数与声明的尺寸把关，解压中再按实际字节累计。
  以前没有任何上限，一个解压炸弹就能吃光引擎的内存；尺寸造假的包还会让导入永远停在「导入中」。
- **MCP 配置不再被覆盖或抹掉**：
  - `servers.json` 读坏（格式有误、读不出来）时拒绝一切写入，设置页给一条横幅、开关失败会回滚——
    以前下一次添加或从市场安装，就用只含新条目的配置把整份文件覆盖了；
  - 应用开着时在外面手改的 `servers.json`，不再被内存里的旧副本盖掉；文件修好之后不用重启；
  - 编辑只提交改过的字段：只改备注不再丢掉 env / headers / cwd / 超时和停用状态；改名原位进行，不再先删后加；
    同名新建在界面上就拦下；参数改成每行一个，`C:\Program Files\…` 这种带空格的参数不再被拆成两段；
    试连用已存的设置加上表单里的改动（以前拿不到已存的 env，要环境变量的服务器在编辑表单里试连必然失败），且不写盘；
  - 从市场更新 MCP 服务器，保留你的启停、备注、cwd、超时与 headers，不再把停掉的服务器悄悄重新启用；
  - 设置页上的改动即时生效成了真话：停用的服务器当场拒绝调用，改过配置的下一回合按新配置重连；
  - 保存不再悄悄丢东西：`servers.json` 里认不出的条目、`mcpServers` 以外的顶层键（比如 `$schema`）原样写回；
    参数与环境变量写成数字或 true / false 的（比如端口 `8080`），以前读进来就被丢掉，现在按字符串保留。
- **数据库比应用新时一个字节都不写**：用旧版本打开新版本写过的库，会弹「这份数据来自更新版本的 DeskMinis」并且只能退出，
  不再把库迁移坏；每条迁移在事务里执行，失败就回滚。
- **开发态数据隔离**：`npm run dev` 改用 `%APPDATA%\DeskMinis-dev` 与凭据服务名 `DeskMinis-dev`，
  不再和装好的正式版共用一个库、一套凭据与配对身份（想把旧数据带进 dev 的手工步骤见 README）。
- **数据目录读写收窄**（文件工具）：以前文件工具对整个数据目录免审读写。现在会话库、模型与搜索服务配置、配对记录、
  本机连接凭据这些核心数据，文件工具一律不能写（完全访问档也不行）；文件工具改 MCP 配置、技能与记忆文件，读会话库或 MCP 配置，
  碰其它会话的文件，都要过权限卡（完全访问档照档位放行），卡上写明这次动的是什么；工作区绑到用户目录这类大目录时，
  文件搜索跳过数据目录并注明。shell 命令与 MCP 工具不走这道闸（只读 shell 命令点到数据目录时怎么判见下一条），见「已知边界」。
- **免询问的只剩只读的本地命令，联网照常要问**：dir、Get-Content、rg、`git log` 这类只读命令（含 `git log | select-object -first 20`
  这类受限只读管道）不弹卡直接执行（0.1.1 没有这份免询问的名单，shell 命令都要确认）。会联网或访问别的机器的照常过卡：`Invoke-WebRequest`、`curl`、
  `git clone` 这类本来就要问；`npm view` / `npm outdated`（要连 npm 源，`npm view` 还能直接取任意网址）、点到远程共享路径
  （`\\主机\共享`、`//主机/共享`）的、带 `-ComputerName` 这类远程参数的（还有 `systeminfo /S`、`Get-Help -Online`），虽是只读写法也不免询问；
  读 `env:` / `$env:` 环境变量的、命令里写着 DeskMinis、`%APPDATA%`、`%LOCALAPPDATA%` 的只读命令同样过卡。`web_fetch` / `web_search`
  单列一档，前两档下每次确认。以上在完全访问档下都不问；判断只看命令写法，认不出的写法见「已知边界」。
- **进程树回收**：点停止、删除会话、退出时，shell 连同它起的子进程整棵回收，不再在后台留下还在跑的程序；
  PowerShell 与 cmd.exe 改用 System32 下的绝对路径并隐藏窗口（以前按裸名找，工作区里放一个同名 exe 就会被执行）；
  shell、终端与 MCP 子进程不再继承 DeskMinis 自己的 `DESKMINIS_*` 环境变量。
- **单实例与数据目录锁**：再开一次只把托盘里的窗口叫回来，不再起第二个；同一个数据目录上的第二个引擎当场拒绝，
  弹「DeskMinis 已在运行」（以前两个实例会同写一个库）；异常退出留下的陈旧锁自动接管。
- **删除还在跑的会话**：先让它停下、等收尾再删，10 秒停不下来就不删并报错。以前直接删：挂着的权限卡没人了结，
  删完再点「允许」工具照样执行，回合还往已删的会话里写消息；左栏删除现在有「删除中」和失败原因。
- **优雅退出**：退出与「重启并安装」都先请引擎有序关停——了结挂着的权限卡、等回合收尾、收掉终端、shell 与 MCP 的整棵进程树（最多等 1 秒）、
  关库、放锁，5 秒不退才强杀。以前直接杀进程：MCP 与 PowerShell 子进程成了孤儿，卡在权限上的回合留下没有结果的工具调用。

### 失效与界面诚实

- **上下文压缩止血**：以前压缩每次都从头取材、不看上一份摘要，失败被静默吞掉且每轮再付费重试一次，空摘要还会被写成
  「[摘要为空]」。现在接着上一份摘要只压新增的部分并按窗口封顶，空摘要与被截断的摘要拒收，
  失败时对话流里给一条提示、回合照常继续；自动取名的输出上限从 64 放宽到 2048（新一代 Claude 关不掉思考，64 几乎总是空标题）。
- **上下文超窗不再谎称「所有模型均不可用」**：超窗与限流、密钥失效分开判断，只降级到窗口更大的模型；
  没有更大的就提示「上下文已满」，并给「新建会话接力」——预填由最近一份摘要与你最后一条消息拼成的接力草稿，由你过目后再发；
  降级提示写明「因上下文已满改用谁」。
- **新一代 Claude（Fable 5.1 / Mythos 5.1 / Opus 5.5）的思考块绑定**：官方端点带 beta 头并发 `drop_block`，
  不再发这三个模型会拒收的 `budget_tokens`；绑定类报错不再被当成普通 400 去换模型；第三方端点遇到绑定报错，
  剥掉历史里的思考块重试一次；报错先写中文说明，不再给一个点了也没用的「重试」。
- **DeepSeek V4 多轮工具调用不再 400**：历史里的 `reasoning_content` 按要求回传（只对 deepseek-v4 族）。
- **卸载的大结果真能取回来**：`file_read` 读回本会话卸载的文件，不再被再卸载成另一个桩（以前照桩去读永远只拿到下一个桩），
  读回按 5 万字封顶；`file_read` 支持 `offset` / `limit` 分段读取，读回截断与超过 1MB 的提示都改成给分段读法，
  不再叫模型用 shell 去读数据目录（那样每次都弹权限卡）；修剪桩不再许诺「原文在卸载文件里」这种空头支票。
- **技能太多列不下时**，提示改指免审的文件工具，不再叫模型用 shell 列数据目录里的技能目录（每次都弹卡）。
- **长单行不再卡住引擎**：URL 里的凭据（`scheme://用户:密码@`）打码改成线性扫描，一条 20 万字符的单行
  从二十多秒降到毫秒级，结果与原来逐字相同；权限审计落盘用的是同一个实现。
- **按会话跟踪运行状态**：切到别的会话再切回仍在跑的会话，停止键回来了，流式区显示「仍在运行」的占位。
- **权限卡按会话区分**：只显示当前会话的卡；别的会话在等你批准时，对话流顶部提示「另有 N 个会话在等你批准」，
  点「去批准」切到等得最久的那个会话；左栏那一行标一个盾牌。
- **断线横幅**：界面与引擎的连接断开时，顶栏下方出一条横幅「与后台服务的连接已断开……」，「重启应用」走优雅退出后重开整个应用；
  断线时清掉已经失效的权限卡、发送键置灰（按 Enter 只提示连接已断开，字留在框里），已经流出来的文字留着，这时点别的会话，
  当前视图原样不动。「重启并安装」之后应用若没有退出，3 秒后兜底退出，不留下引擎已停、窗口还开着的状态。
- **欢迎页选助手不再撒谎**：有空会话开着时在欢迎页选助手，也会真的套用到这个会话上
  （以前界面显示「已选 X」，发出去却没套用——只有没有会话时才生效）；打开会话时按它绑定的助手高亮。
- **模型切换条标明「默认模型」**，输入卡的模型胶囊在会话没绑定时显示「默认 · X」。
- **外链交给浏览器，窗口只开自己的页面**：对话里的网页与邮件链接交给系统默认程序打开；终端里的超链接先弹中文确认框，
  写明网站与实际地址，确认后交给系统浏览器。应用窗口拦下站外导航与新窗口，网页权限只放行本应用页面的剪贴板写入；
  打包版只加载自己的页面，不认开发用的环境变量 `ELECTRON_RENDERER_URL`（以前外面设了它，打包版就去加载那个地址）。
- **打包版去掉重载与开发者工具的快捷键**：回合中途误按 Ctrl+R 不再重载界面（那会丢掉流式正文、待批的权限卡与没发出去的字），
  Ctrl+Shift+I 也不开开发者工具；复制粘贴、Ctrl 加 + / - / 0 缩放与 F11 全屏照常。
- **本地崩溃记录与按天日志**：两个进程的未捕获异常记进 `crashes.json`，引擎的输出与启动、退出的经过按天写日志，
  都在 `%LOCALAPPDATA%\DeskMinis\logs`（开发态为 `DeskMinis-dev`），出了问题有据可查；引擎里漏接的异步错误只记下来、
  不再让整个引擎退出，主进程出错时弹框写明记录在哪；权限询问超时那一刻审计写不进库，也不再把引擎打死。
- **历史里没做完的工具标「已中断 · 结果未知」**：回合已经不在运行、却没有结果的工具步骤不再画成成功；
  终端、定时任务、设备页的说明按实情改写。
- **右栏「改动」只列真改成了的**：写失败的、历史回合里中断后没有结果的写入不再列成改动（运行中的回合里还没出结果的照旧先列上），
  与对话流里的步骤状态用同一套判定。
- **规则拦下的命令不再说成「被用户拒绝」**：危险命令被规则拦下时，回给模型的话写明是规则拦的、换任何档位也不放行，
  并提示：要读的文件、要搜的文本里恰好含这些字样的话，改用 `file_read` / `file_grep`（以前一律说「命令被用户拒绝（可在设置-权限中调整）」，
  用户其实没点，设置里也调不开）。设置页「完全访问」的说明不再许诺「不可逆的系统操作仍拦截」，改为危险规则只按命令写法拦、
  拦不住所有不可逆操作；「每次确认」的说明写上只读的本地命令也直接放行。
- **定时任务失败不再记成 ok**：模型报错或回合失败时，任务的「上次」状态记为出错。
- **关掉记忆的会话，记忆工具也调不动了**：以前只是不把记忆工具列给模型，模型照着历史里的调用再叫一次，照样会读写记忆日志。
- **设备同步握手带协议版本**：两端交换协议版本，缺失时按 0.1.1 处理，与装着 0.1.1 的设备仍能互相同步。
- **自动更新改走公开发布仓库**：以前的更新源是私有源码仓，检查永远 404。现在检查 `lincheuk/deskminis-releases`；
  失败原因译成一句中文，不再露英文堆栈和响应头；便携版不检查、不下载；托盘「检查更新…」给结果回执。
- **新版下载完成的提示说实话**：以前写「现在重启即可用上新版本……下次启动时再装」，关于页写「重启后生效」，
  其实退出和重启都不会装。现在说清：点「重启并安装」会关闭 DeskMinis、打开安装程序，保持默认选项往下点，
  最后一页点「完成」就打开新版；关掉不会自动安装，之后从托盘「检查更新…」或关于页「现在检查」可以再装。
  更新过程（发现新版、已是最新、下载完成、点了「重启并安装」、出错的原文）写进按天日志，每行带 `[update]`。
- **许可改为 Apache-2.0**（此前 package.json 误写 ISC），安装目录 `resources\` 下附 `LICENSE.txt` 与
  `THIRD-PARTY-NOTICES.md`（第三方署名，含随包字体的 OFL 声明）。
- **发布校验**：`npm run verify:release` 核对要上传的四件——`latest.yml` 与安装包的 sha512 和大小、blockmap、便携版，
  以及写进安装包的更新源与随包许可；`npm run smoke:release` 一条命令跑发版冒烟（真 key，缺 key 的用例跳过）。

### 修复

- **权限卡在 0.2.0 之后的界面重做中被漏掉了**：默认权限档位就是「每次确认」，
  而新界面从没渲染过权限请求——agent 一请求权限，界面上什么都不出现，
  回合无声卡死到 90 秒超时后被判拒绝。本版补回，含批准前的差分预览。
  （同批补回的还有：事件提示条与出错重试入口、推理过程展示（DeepSeek 这类经 OpenAI 兼容端点返回 `reasoning_content` 的模型）、
  图片附件的粘贴 / 拖拽 / 选择三条入口与历史消息里的附件、文本选区注释与引用。）
- 设置页显示的「当前默认模型」一直是猜的（取列表第一个），可能与后端实际使用的不是同一个。
  改为从后端读回。
- 更新检查状态、上一轮结束原因等内部标识符会原样显示给用户（如 `endTurn`），已全部汉化。
- 打不开的 Office 文件会把英文原始报错直接显示在预览卡上，已改为中文说明。
- 右栏三个标签页在窄栏下会被折成两行。
- **绑定自定义工作区后文件面板整个打不开**（报「只允许访问会话工作区」，一个文件都列不出来）。
  围栏基准写死成了会话沙箱目录，而路径解析认的是绑定目录，两边对不上。
  这个从 0.1 时代加工作区绑定功能起就坏着，因为没有一条测试把「绑定目录」和「文件面板」连起来测。
- **工作区绑定的界面入口在界面重做后丢了**（后端一直是通的）。本版补回：
  右栏「文件」tab 上方一行，支持原生选择器与粘贴绝对路径两种方式，绑定后新建会话会继承。
- **首条消息的两处首发缺陷**（排查「紧跟启动的首条 Enter 偶发被吞」时挖出；那条记录本身是
  测试剧本的误判，消息从未丢过）：① 首条消息要先建会话，这几十毫秒里再按一次 Enter 或点发送键
  会建出两个会话、弹「该会话正在运行中」红横幅、停止键消失——现在发送期间发送键变灰、第二次按键不生效；
  ② 全新安装还没配模型时，第一句话按下 Enter 会被静默吞掉（文字消失、只多出一个空会话、没有任何提示）——
  现在欢迎页输入卡会显示原因，输入的文字留在框里。
- **界面重做时漏接的入口成批补回**（能力与数据一直都在，只是点不到）：会话行 ⋮ 菜单（重命名 / 删除 /
  记忆开关 / 绑定模型）、输入卡「MCP」胶囊对单个会话禁用某台、设备页暂停 / 恢复同步（标题栏状态点同步变橙）、
  助手绑定默认技能、右缘锚点轨一键跳回合，以及托盘菜单的「打开设置」「切换右栏」两项（重做后点了没反应）。
- **输入框下方的模型胶囊只会显示默认模型**，不管这个会话（或选中的助手）绑定的是哪个——
  现在显示这条消息实际会用的模型或模型组；绑定对象被删了会变色提示，而不是照常显示。

### 已知边界

- **从 0.1.1 升级要手动装一次**：0.1.1 写死的更新源是私有源码仓，永远查不到新版；手动下载安装 0.3.0 之后才走新的更新源。
- **便携版不自动更新**，新版请到发布页下载；便携版与安装版共用 `%APPDATA%\DeskMinis`，不能同时运行。
- **安装包没有代码签名**，Windows SmartScreen 会提示「未知发布者」，选「更多信息 → 仍要运行」。
- **卸载不删 `%LOCALAPPDATA%\deskminis-updater`**：安装程序留在这里的自身副本与自动更新下载的安装包，卸载后还在，要彻底清理请手动删。
- **「每次确认」与「本会话沿用」两档**在后端判定相同，差别只在权限卡上预选的按钮（「允许」或「本会话允许」）。
- **数据目录的闸只管文件工具**（`file_*`、`office_*`）：shell 命令与 MCP 工具不走它，照常按档位过卡，完全访问下不问——
  这时 agent 用 shell 改写会话库、模型与搜索服务配置、配对记录也不会被拦；只读 shell 命令用 `..\..\..` 这类相对路径
  （默认工作区就在数据目录里）或 `DeskMi*` 这类通配点到数据目录时认不出，照常免询问。
- **危险命令只按写法认**：Remove-Item、del、format、shutdown 这类在任何档位都直接拒绝，但经 `cmd /c` 转一手，
  或 Format-Volume、Clear-Disk、`[IO.File]::Delete` 这类规则表里没有的破坏性写法认不出——前两档照常弹卡，完全访问下直接执行。
- **只读命令的间接读取认不出**：先用 `-OutVariable` 这类公共参数把内容存进变量、或用 `findstr /F:清单文件` 从文件里取路径，
  下一条只读命令就能读到远程共享或 `env:` 路径而不询问；映射成盘符的网络驱动器也按本地路径算。
- **部分克隆（partial clone）的仓库**里，只读的 git 命令会按需连远端取对象，不询问。
- **MCP 服务器的 env / headers 明文存在 `mcp-servers\servers.json`**（含扩展市场确认卡里填的密钥）；值可以写成 `$$变量名`
  引用环境变量、连接时才取值，但界面上没有这个写法的提示。
- **左栏收起时看不到别的会话在等批准**：提示行只在对话页顶部，盾牌标在左栏的会话行上；停在欢迎页、设置页这类页面又收起了左栏时，
  两处都看不到，那张卡 90 秒没人批就按拒绝处理。
- **思考档位还没有界面入口**，界面不发思考档位。经 RPC 带着思考档位调用时，除 Fable 5.1 / Mythos 5.1 / Opus 5.5 外，
  Opus 4.7 / 4.8 / 5、Sonnet 5、Fable 5 仍会被发 `budget_tokens`（这几个模型会拒收，请求报 400）。
- **系统提示仍每步重建**，会改动对话前缀：新一代 Claude 在第三方端点上的新账号靠「剥掉思考块重试一次」维持，
  官方端点靠 `drop_block`，首轮的成本与延迟会上升。
- **上下文超窗**时只会降级到窗口更大的模型，或报「上下文已满」给出接力，还没有「强制压缩后在同一个模型上重试」。
- **压缩是止血版**：失败会提示、不再静默，但长会话的压缩质量要等 0.4.0 的引擎重写。
- **引擎崩溃或被强杀时，终端、shell 与 MCP 再起的进程会留下**（`npx` 拉起的 MCP 服务器、终端里的 dev server、shell 里的 `ping -t` 这类）：
  正常退出会把整棵进程树收掉，崩溃时来不及。遇到时在任务管理器里结束它们。
- **只读的 git 命令会读取仓库自己的配置**，配置里可以指定外部程序；工作区里的文件 agent 可以免审改写，
  所以「每次确认」档下 git 的只读子命令不能保证不牵动别的程序。收窄排在之后的执行面加固里。
- **`servers.json` 里认不出的条目**（比如 command 写成了数组）保存时原样留在文件里，但设置页不列出、也不提示；要用它得在文件里改对。
- **MCP 配置里写成数字的参数与环境变量按数值读**：超过 2^53 的整数、末尾带 0 的小数（`3.10`）保存后会变样（`3.1`），这类值请加引号写成字符串；
  环境变量写成 `false` 会按字符串 `"false"` 传给服务器，多数程序会把它当成真。
- **删除会话不删磁盘上的 `sessions\<id>\` 目录**；MCP 服务器改名不迁移会话级的禁用名单。
- **数据库降级守卫**只对装过 0.3.0 之后再回退的情况有效（更早的版本没有这道闸）。
- **设备同步**默认只监听本机，跨机器要在两台机器上都设环境变量 `MINISD_HOST`，界面暂无开关；记忆文件不同步。
- **设备同步不传播删除**：一台机器上删掉的会话，下次与已配对设备同步时会回来。
- **数据目录是 UNC 路径**（`\\server\share\…`）时，文件工具按相对路径处理并以穿越拒绝，读不到技能与卸载文件。
- **技能索引里的路径按 XML 转义**：用户名含 `&` 时，模型照抄进 `file_read` 会失败（旧问题）。
- **URL 凭据打码**：多行文本（工具结果、记忆、摘要）是先打码、后剥 `\v`、`\f` 这类控制字符，
  凭据本身夹着这类字符时口令不打码（现实里碰不到）。

## 0.2.0 — 2026-08-20（未公开发布）

没有发布过（此前的 Releases 里只有 0.1.1，发在私有源码仓，外人看不到），这一节的内容随 0.3.0 一起首发。

界面重做 + 助手 / 定时任务两大新面 + 一批输入体验补强。

### 界面
- 全新蓝白视觉（参考 [AionUi](https://github.com/iofficeai/aionui) 的界面语言，Apache-2.0）：
  欢迎屏「你好，今天想做点什么？」→ 输入卡 → 助手卡片流；侧栏默认展开带品牌行；
  标题栏收敛为单 ☰ 菜单；深浅双主题全量对齐，对比度守卫（26+1 对）随行。

### 新能力
- **助手体系**：命名预设（名称 / emoji / 规则 / 默认技能 / 默认模型）。欢迎页选中助手
  再输入即创建绑定会话并注入规则；设置 → 助手可增删改；内置「通用协作 / 代码助手 /
  文档写手」三个种子（删除后不会复活）。
- **定时任务**：每 N 分钟 / 一次性 / cron 三种调度，可绑定助手，支持立即运行、状态回流
  与「查看会话」跳转。诚实边界：关掉窗口后应用仍在托盘里运行、任务照常触发；从托盘退出或关机后不再触发，
  错过的任务下次启动时只补跑一次。无人值守时权限询问 90 秒自动拒绝——要全自动请把权限档切到完全访问（慎用）。
- **输入与导航**：↑ 召回输入历史（有草稿不抢）；`@` 引用工作区文件路径（模糊匹配，
  深度 / 数量受限的快速索引）；消息右缘锚点轨一键跳回合；文件面板 `.md`
  渲染 / 源码切换预览；会话级 MCP 禁用有了界面入口（输入卡「MCP」胶囊）。
- **0.1.1 之后加入、这一节原先漏记的**：扩展市场（ClawHub、官方 MCP Registry、awesome-dsh-plugin 三源）、
  MCP（stdio / streamable-http）、选区注释、图片输入、`file_list` / `file_glob` / `file_grep` / `web_fetch` / `web_search` 五个工具，
  以及常见只读命令免询问（现行规则见 0.3.0「数据安全」）。

### 修复
- 会话绑定模型此前从未真正生效（存的是裸 provider id，后端只认前缀格式）——三侧齐修，
  旧数据兼容；绑定失效改为可见报错，不再无声回落默认模型。
- 会话级 MCP 服务器列表在应用刚启动时拉取失败导致入口不出现——改为会话就绪后重试。

### 已知边界
- 自动更新指向私有源码仓，检查一直 404（被静默处理，不打扰使用）——0.3.0 起改走公开发布仓库。
- 助手的技能勾选是建会话时的快照，会话建成后新装的技能按全局开关生效。

## 0.1.1

打包与分发首版（发在私有源码仓的 Releases 里，外人看不到）：NSIS 安装包 + 便携版、GitHub Releases 自动更新接线
（更新源写死成私有源码仓，实际一次也查不到新版；0.1.1 的用户需要手动安装一次 0.3.0）、随包桥运行时（PowerShell 桥 / .cmd 垫片）。

## 0.1.0

首个可用形态：Agent 循环与工具集、多 Provider 接入、三档权限网关（0.1.x 的三档只在界面上，0.2.0 才接到引擎）、持久记忆与上下文
压缩、技能系统、每会话工作区、设备内网直连同步、内嵌终端 / 文件树 / 产物面板。
（扩展市场、MCP、选区注释是 0.1.1 之后才加的，见 0.2.0 一节。）
