# CROSS-CHECK

## file_conflicts
- src/main/index.ts 有 8 项要改，必须按这个顺序串行：W1a-dbguard（:48-66 stdout 循环识别 fatal 行 + stderr 4KB 环形缓冲；:222-228 catch 弹框）→ W1a-devdata（:5/:14 模块顶层 resolveAppDirs + setPath；:35 fork env；:200 attachments:save）→ W1b-single（顶层 requestSingleInstanceLock + second-instance；:8 新增模块级 mainWindow；:206 whenReady 首行早退；:216 赋值）→ W1b-quit（:17 stopMinisdGracefully；:66 之后常驻 exit 监听；:144 update-downloaded；:230 before-quit）→ W2b-relaunch（:177 旁 app:relaunch）→ W2b-guards（:85-96 createWindow，:206 设置 session handler）→ W2b-crashlog（:60/:65 写日志，:66 exit 监听复用 W1b-quit 那一个，:206 安装钩子）→ W2b-update（:79、:146-162）。其中 dbguard 和 crashlog 各设计了一个 stderr 环形缓冲，W1b-quit 和 crashlog 各设计了一个 exit 监听：各实现一次，后来的项复用，不要重复造。
- src/minisd/index.ts 共有 9 项改动，按以下顺序串行：MCP-corrupt/stale（:837）→ MCP-edit（:1、:3、:862-870）→ W1a-dbguard（:1229-1234 standalone catch）→ W1a-devdata（:247 KeyringVault）→ W1b-single（:231-235 加锁；close 释放锁；fatal 行加 DATA_ROOT_LOCKED）→ W1b-delete（:281 denyPendingPerms，:404-408 runs Map，:517-522，:673-758）→ W1b-quit（:582/:1131 closing 闸，:1206-1216 close 重写，:1225-1228 parentPort）→ W2b-welcome 后端（:467 旁 applyAssistant）→ W2b-crashlog（:1220 standalone 钩子）。另外，W1b-single 原方案要「把 :235 起整段装配包进 try」，这会让 startMinisd 近 1000 行整体重缩进，与上面每一项都冲突。建议改为：把现有函数改名为 assembleMinisd(root, lock, opts)，函数体不动；另写一个外壳 startMinisd，依次做 mkdir、acquire，再 `try { return await assembleMinisd(...) } catch (e) { lock.release(); throw e }`。
- src/renderer/src/stores/chat.ts 有 8 项改动，按以下顺序串行：MCP-corrupt（:77-84、:335-339）→ W1b-datagate（:10 PendingPerm 加 note，:155-161 push 时带 note）→ W2a-compact（:106 联合类型末尾追加 'compactFailed'，:566 之后加分支）→ W2b-runset（:95、:134-143 trackRun、:397、:447、:456、:519-537）→ W2b-permscope（:10 加 sessionId，:155-161，:165-170）→ W2b-disconnect（connection、markConnectionLost、relaunchApp，init 在 connect 之前注册 onLost）→ W2a-overflow UI（:528 之前的 contextFull 分支、:540-548 fallback cause、relayDraft、relayToNewSession）→ W2b-welcome（:224-228 applyAssistantToSession/ensureSession、:401）。datagate 和 permscope 改的是 :10 同一个接口、:155-161 同一个 push 对象，后合入的一方保留两个字段。
- 引擎分区和渲染分区各写了一套 W2a 溢出界面，改的是同一批位置：stores/chat.ts 的 relayDraft 与 error 分支、EventNotes.vue:24-27/:39、Composer.vue:259-260、lib/eventnote/copy.ts，两套契约互不相容。字段差异：code 一边是 'contextFull'，一边是 'context_overflow'；一边传 relayDraft 全文，一边传 relaySummary；eventNotes.relay 一边是 string，一边是 boolean；store.relayDraft 一边是 string，一边是 {sessionId,text}；消费方式一边用 watch(immediate)，一边只在 setup 里消费。这部分必须由一个人按统一契约实现（契约见 corrections），两份守卫 tests/renderer-context-full.test.ts 和 tests/renderer-overflow-relay.test.ts 合并成一个文件。
- src/renderer/src/ui/Composer.vue 有 4 项改动，按以下顺序串行：W2b-disconnect（:33 canSend，新条件插在尾锚 `&& !chat.running && !sending.value)` 之前）→ W2a-overflow UI（setup 顶层的 takeRelay 放在 :260 takeDraft() 旁，函数定义放到 quote() 之后，避开 first-send 守卫的 sendBody 切片）→ W2b-welcome（:43-46 effectiveBinding 改走 previewBinding，:181-185 改调 ensureSession，:225-234 加 else 分支）。
- EventNotes.vue 和 lib/eventnote/copy.ts：W2a-compact 往 :16-23 的 ICONS/TONES 和 copy.ts:31-40 加 compactFailed；W2a-overflow UI 要加接力按钮和 n.short，并在 copy.ts:23 的 5xx 正则之前加溢出识别。顺序是 compact 先、overflow 后。
- src/minisd/agent/loop.ts 有 5 项改动，按以下顺序串行：W2a-compact（:15-27 加 compactFailed，:321-342）→ W2a-overflow（:23 fallback 加 cause，:27 error 加 code/relayDraft，:427 之后拦截 contextOverflow，:462）→ W2a-claudebinding（在 :427 之后紧挨着拦截 thinkingBinding）→ W2a-deepseek（:66-77 toAgentMessages 改走 agent-message.ts）→ W2a-honest（:557-562 读回分支）。overflow 和 claudebinding 改的是 providers/types.ts:17-29 同一个构造器，code 字段的联合类型应在 overflow 那一步一次定为 'contextOverflow'|'thinkingBinding'。
- compact.ts / context-policy.ts / prune.ts / anthropic.ts 的顺序：compact.ts 先由 W2a-compact 改 :48-98，再由 W2a-deepseek 把 :105-137 改走 toAgentMessage；context-policy.ts 先由 W2a-compact 导出 estimateTextTokens、windowOf、FALLBACK_WINDOW，W2a-overflow 直接复用，不要各写一份；prune.ts 先由 W2a-deepseek 把 :41 改成 {...m, parts}，再由 W2a-honest 改 :39 桩文案；anthropic.ts 先由 W2a-compact 改 :93 STOP_MAP，再由 W2a-claudebinding 改 :1-47 和 :100-112。
- src/minisd/tools/files.ts：W1a-edit 改 :118-123（抽出 edit-text.ts），W1b-datagate 改 :17-49 的 guardWrite/guardRead。两处在同一文件，先合 edit 再合 datagate。src/minisd/tools/shell.ts：W1b-killtree 改 :56-64、:151-178，W1b-delete 要在 :173-177 旁新增 ShellManager.dispose(sessionId)。建议 dispose(sessionId) 直接并入 killtree，delete 只调用它。
- src/minisd/paths.ts：W1a-devdata 在 :9-13 抽出 defaultDataRoot，W1b-datagate 在 :4-7 导出 SESSION_BUCKETS/GLOBAL_DIRS。devdata 先，datagate 后。建议 datagate 同时在这里导出保留文件名常量 minisd.lock、minisd.lock.recovery，W1b-single 引用它，免得两处各写一份文件名。
- mcp/config.ts 和 SecMcp.vue：W1a-mcpcorrupt 在 upsert/remove/toggle 首行插 assertWritable；W1a-mcpstale 在同样的首行插 syncFromDisk，并且要排在 assertWritable 之前；W1a-mcpedit 重写 :152-211。顺序为 corrupt → stale → edit。SecMcp.vue 由 corrupt 改 :82-95、:119，再由 edit-ui 改 :11-74、:141-145。
- THIRD-PARTY-NOTICES.md（仓库根 /home/user/Deskminis/）会被 5 个分区追加：W1a-edit（pi edit-diff）、W1b-killtree（pi shell.ts）、W1b-single（ZCode lock.ts）、W2a-overflow（pi overflow.ts）、W2b-crashlog（pi crash-log）。W2b-license 还要改 §1-§3 并新增 AionUi 一节。先由 W2b-license 定好格式（每个上游一节加一张表）和双向绊线测试；之后各步只往 pi-mono 节或 ZCode 节的表里追加行，不要各自新开一节。
- package.json 与 electron-builder.yml：W2b-license 改 package.json:29 和 extraResources :19-23；W2b-verifyrelease 在 package.json 加 scripts；W2b-update 改 electron-builder.yml:59-62 的 repo。三者按 license → update → verifyrelease 的顺序合入，verify-release 的 ⑦（app-update.yml 对照 publish）和 ⑧（LICENSE.txt）依赖前两步。
- 测试文件的交叉点：tests/ipc-contract.test.ts:16-19 的 electron 桩，W1a-devdata 要 setPath，W1b-single 要 requestSingleInstanceLock，建议 devdata 那一步把 setPath、requestSingleInstanceLock、relaunch、exit 一次补齐，W2b-guards 再补 shell 和 session。tests/renderer-model-groups.test.ts：W2b-disconnect 改 :15 桩补 onLost，W2b-welcome 重指 :178。tests/renderer-permcard.test.ts：datagate 追加 note 守卫，permscope 必须保留 :134 的字样。tests/auto-update.test.ts:55：W1b-quit 必须让 `quitAndInstall();` 与收尾的 `}` 留在同一行，W2b-update 在同一个文件里追加断言。
- README.md:28（MCP 分区）与 W2b-readme 改的是同一个文件，:28 并入最后的 README 那一步一起改。SecMcp.vue:80「改动即时生效」只要 manager 还不断连，就是一句假话，也放在 README 那一步统一处理（或者修 manager，见 missing）。

## dependencies
- W1b-datagate 与 W1b-single：硬拒表必须包含数据根锁文件 minisd.lock 和 minisd.lock.recovery（W1b-single 写在 <dataRoot> 下），否则 agent 能用 file_write 删改锁文件。W1a-devdata 的 <DATA_DIR>/electron（dev 下的 userData）和 W2b-crashlog 的 <DATA_DIR>/logs 也落在数据根内，按「其余」分支走卡，这一点要在 dataGate 注释里写明。
- W1b-datagate 与 W2a-honest：卸载读回依赖「读当前会话的 offloads 桶免审」。datagate 方案（C）已把当前会话各桶列为免审，data-root-write-gate.test.ts 的 ⑨ 要加一例 file_read /var/minis/offloads/x.txt 零卡，把这条钉住。
- W1b-datagate 与 W2b-permscope：前者给 PendingPerm 加 note，后者加 sessionId，改的都是 stores/chat.ts:10 和 :155-161。PermCard.vue 的 note 行与孤儿卡的「来源会话不在列表中」提示位置相邻，需要统一排版后再做 xvfb 目视。
- W1a-mcpcorrupt/stale 与 W1b-datagate：datagate 之后，agent 写 servers.json 要过权限卡（mcp-servers/** 走卡）。批准之后，stale 的 refresh 让改动在下一次 list 时生效；corrupt 保证 agent 写坏的文件不会被界面或市场覆盖。datagate 那张卡的 note「命令会在下次连接时执行」是否属实，取决于 stale 是否已经落地。
- W1a-mcpedit 依赖 W1a-mcpcorrupt：compose 与 renameFrom 都在 assertWritable 之后执行。W1a-mcpedit-ui 依赖 mcpedit 后端：前端要发 renameFrom 和 note:null 这两种载荷。mu6-capability-wiring.test.ts:144-150 规定不得新增 store action。
- W1b-delete 依赖 W1b-killtree：要用 ShellManager.dispose(sessionId)，而且 dispose 要走 killTree。依赖顺序是「run 收尾 → 终端/shell dispose → deleteSession」。delete 的渲染端配套（NavRail.vue:54-57 加 try/catch 和删除中状态）没有分区认领。
- W1b-quit 依赖 W1b-delete 和 W1b-single：close() 复用 delete 引入的 runs Map、denyPendingPerms、stopRun（index.ts:281、:404）；锁文件的释放放在 close 的最后一步（W1b-single）。auto-sync.test.ts:271-282 要求 close 后同一个根能重启，所以 release 必须幂等。
- W1b-single 依赖 W1a-dbguard 和 W1a-devdata：DATA_ROOT_LOCKED 复用 dbguard 的 minisdFatal stdout 行和 main/minisd-fatal.ts；userData 的 setPath 必须早于 requestSingleInstanceLock。另外，未打包且设了 DESKMINIS_DATA_DIR 时，如果不把 userData 挪到 <DATA_DIR>/electron，driver 会被当成第二个实例直接退出。
- W2b-relaunch 依赖 W1b-quit（before-quit 里的优雅停止）和 W1b-single（模块级 mainWindow，用来校验 e.sender）。W2b-disconnect 依赖 W2b-relaunch 的 preload relaunchApp 和 ipcMain.handle('app:relaunch')，ipc-contract.test.ts:72-81 会自动检查二者成对；它还依赖 W2b-runset 的 runningSessions，markConnectionLost 要清空这个字段。
- W2b-crashlog 依赖三处：W1a-devdata（app-dirs 给出 logRoot，并经 DESKMINIS_LOG_DIR 下发）、W1b-quit（常驻 exit 监听，退出流程中的 exit 不记为崩溃）、W1a-dbguard（stderr 环形缓冲）。
- W2a-overflow（引擎）与 W2a-overflow UI（渲染）：LoopEvent 契约要先由引擎那一步定下并加测试钉住，渲染端只按 code 分流，不靠文案正则判断。W2a-compact 的 compactFailed 事件同样需要渲染端配套：chat.ts:106、copy.ts、EventNotes.vue，引擎分区已经把这三处列进去了，渲染分区没列，不能重复实现。
- W2a-overflow UI 与 W2b-welcome：relayToNewSession 以 chat.sessions.create {assistantId} 建会话后 open()。W2b-welcome 把 chat.ts:401 改成「open 时从会话已绑的助手初始化 welcomeAssistantId」之后，新会话欢迎页的助手高亮和胶囊才正确，也不会重复套用助手。
- W2b-welcome 依赖后端 RPC chat.sessions.applyAssistant（放在 minisd/index.ts:467 旁，复用 applyAssistantPreset 和 skills setSessionOverride(null)）。没有任何后端分区认领这个 RPC；mu6-capability-wiring.test.ts:91-96 的 WIRED 一旦登记，后端缺这个方法就会变红。
- W2a-claudebinding 与 W2a-deepseek 依赖 tests/provider-body-golden.test.ts：这个黄金快照必须先单独提交并保持全绿，然后才能改 anthropic.ts 和 openai.ts。W2a-claudebinding 与 W2a-overflow 共用 ProviderError.code（types.ts:17-29），也共用 loop.ts:427 之后的拦截位置。
- W2b-license 的格式与绊线测试依赖 W1a-edit（第一次借用 pi 代码）：为了「借用即登记」，登记格式必须在 W1a-edit 之前落地。license-consistency 的正向扫描认的文件头格式是「改编自 <名>（https://…）」，所以 edit-text.ts、win-exec.ts、data-root-lock.ts、overflow.ts、crash-log.ts 的文件头都必须严格按这个格式写。
- W2b-update 与 W1b-quit 改的是同一段 update-downloaded 与 quitAndInstall（main/index.ts:131-148）。W2b-update 新增的 'portable' 状态，要求 SecAbout.vue 同一个提交里补上对应文案（renderer-stage-views.test.ts:71-78）。W2b-verifyrelease 的 ⑦ 依赖 W2b-update 拍板的仓库名。
- W2b-readme 与界面文案依赖全部代码落地。欢迎页选助手、断线横幅、权限卡按会话区分、数据根收窄、killTree、压缩止血、溢出分类这几行，要等对应步骤合入之后才能标「属实」。同类假话出现在 TerminalPane.vue:63/:108、StageCron.vue:92、StageDevices.vue:87、SecMcp.vue:80，必须同一步改掉，并做 xvfb 目视。
- Linux 上的权限卡触发方式与 W1b-datagate 有关：session-delete-running 和 shutdown-partial-reply 需要在 Linux 上稳定触发权限卡。要么在 datagate 之后改写 /var/minis/skills/x/SKILL.md（这时走卡），要么用 web_fetch：web.ts:117 的 kind 是 web-fetch，permissions.ts:226 定为 askOnce，放行前不出网。POSIX 绝对路径的 file_write 在 Linux 上走不到权限卡，原因见 corrections。
- W1a-devdata 与子进程环境：devdata 往 minisd 的 process.env 注入 DESKMINIS_DATA_DIR、DESKMINIS_KEYRING_SERVICE、DESKMINIS_LOG_DIR。shell.ts:63、terminal.ts:75、mcp/stdio.ts:179 都把完整 env 传给子进程，而 DESKMINIS_STANDALONE=1 现在就已经漏出去了。有两种处理：在 killtree 那一步（已经在改这三个文件）剥掉 DESKMINIS_* 变量；或者接受这个问题并写进风险。

## corrections
- W1b-delete 的 session-delete-running、W1b-quit 的 shutdown-partial-reply (a)、渲染分区的 xvfb 断线与权限卡场景，都想让 FakeProvider 用 `__tool__ file_write {path:<mkdtemp 外部绝对路径>}` 触发权限卡。这在 Linux 上做不到：paths.ts:54-55 会对 POSIX 绝对路径抛「不支持的绝对 guest 路径」，工具直接失败，不会发出 permission.request。已实跑现有的 tests/rpc.test.ts「权限询问超时…」（:246-256，写法相同），结果是「等待超时: permission.request」，这一例本身就在 52 例 Windows 基线里。按原写法，这几条先红测试会进 Windows 基线，拿不到 Linux 上的先红证据。改法：用 web_fetch（web.ts:117 askOnce）；或者等 datagate 之后写 /var/minis/skills/x/SKILL.md。
- W2a-honest 的红测 ②（file_read 宿主绝对路径 join(sessionBucket(sid,'offloads'),'T1.txt')）同样因为 paths.ts:54-55 在 Linux 上跑不通，请删掉，或改成 Windows-only。readBackOf 可以直接做单元测试：Linux 上传入 POSIX 绝对路径时应返回 undefined，而不是抛错。
- 渲染分区 W2b-welcome 的守卫写了「Composer 不再含 `await chat.newSession()`」，但同一方案又要求 send() 里 `if (!chat.activeId)` 的建会话分支原样保留，而 Composer.vue:229 那一行就是 `else await chat.newSession();`，所以这条守卫在方案落地后会一直红。二选一：把守卫限定在 saveImages 的函数体（:181-185）内；或者把 :229 也改成 ensureSession()，而 first-send.test.ts:44 只锚 catch 形态，这样改不受影响。
- 引擎分区的 W2a-overflow 让 Composer 用 `watch(() => chat.relayDraft, {immediate:true})` 消费接力草稿，而且先设 relayDraft 再 await newSession()。这样做有问题：在新会话 open 完成之前，旧会话 StageChat 里那个 Composer（StageChat.vue:242）还挂载着，它会先把草稿取走；接着 AppShell.vue:50 的 inChat 判断切到 StageWelcome，旧组件卸载，草稿随之丢失。应改用渲染分区的方案：relayDraft 设为 {sessionId, text}，只在 setup 中消费，并且要求 sessionId === activeId 才取。建议的统一契约：loop 发出 {kind:'error', code:'contextFull', message, relayDraft}，其中 relayDraft 由引擎用 store 里的完整 marker 摘要拼成，上限 8000 字（渲染端手里只有截成 200 字的 compactedState）；fallback 事件带 cause:'contextOverflow'；eventNotes 加 relay?: boolean 和 short?: string；草稿正文放在 store.relayDraft.text；原会话的工作区和模型绑定按渲染方案照抄。
- copy.ts:13-28 的 humanizeError 会对整条 detail 依次跑 401/403/429 与 /\b(5\d{2})\b/ 这几条正则。W2a-claudebinding 的中文文案后面拼了「（原始错误：Anthropic HTTP 400: …响应体）」，响应体里只要出现一个独立的 5xx 三位数，就会被误判成「模型服务暂时不可用」。渲染分区只针对溢出文案发现了这个问题，其实 thinkingBinding 也会中招。改法：eventCopy 先看事件带的 code（contextFull / thinkingBinding）再决定短句，不对原文跑正则。
- 第 1 分区（工具层）说打包后 userData 就是 %APPDATA%\DeskMinis（「productName 生效」），这不准确。electron-builder 不会把 productName 写进包内的 package.json：fileTransformer.js:88-91 只 deepAssign extraMetadata，而 package.json:2 只有 "name":"deskminis"。所以打包后 userData 是 %APPDATA%\deskminis。第 3 分区（生命周期）的判断是对的。NTFS 不区分大小写，两个路径是同一个目录，结论不变，但 dataGate 在 win32 下必须做大小写不敏感的比较（方案已写小写化）。
- W1b-datagate 的红测 ⑧ 在 Linux 上以 platform='win32' 调纯函数 dataGate，路径形如 'C:\\X\\DeskMinis\\…'。而方案的第（A）步要对「最近存在的祖先」取 realpathSync.native，这些 Windows 路径在 Linux 上不存在，这一步要么抛错，要么走错分支。realpath 必须能注入（例如 opts.realpath），或者在 platform !== process.platform 时跳过，只做 path.win32 的词法规范化，否则 ⑧ 不会按预期变红再变绿。
- W1b-single 原方案说「把 minisd/index.ts:235 起的整段装配包进 try」，这会让 startMinisd 约 1000 行整体重缩进，与 MCP、dbguard、devdata、delete、quit、welcome、crashlog 七处改动全部冲突。应改为保留函数体、改名成 assembleMinisd，外层再包一个 try/catch 负责释放锁（见 file_conflicts）。
- W2a-compact 对 agent-loop.test.ts:888-919 的「保持绿」论证，写的预算是「64K×0.6−4096」，但按它自己的公式，summaryMaxTokens = min(16384, 目录输出上限, floor(64000/4)=16000) = 16000，输入预算只有 38400−16000 = 22400 tokens，而这一例的增量未截断时约 22.5K。结论仍然成立，但前提是逐条截断（单条 15000 字截成 4000+2000 字，6 条约 9K tokens），并不是预算本身够。实现时要复跑这两例，并把耗时控制在 120s 以内。
- 渲染分区在 open_questions 里建议主进程重启时「超时再 app.exit(0)」，这与生命周期分区 W2b-relaunch 的设计和守卫（处理体不许出现 `app.exit(`，要走 app.quit() → before-quit 优雅停止 → kill 兜底）相冲突。以生命周期分区为准。
- 报告 §4 A#9 和 roadmap W1a 都写着「复用 office/zip.ts 的三道闸」，实际只有两道：:14 的 MAX_ENTRIES 与 :15 的 MAX_TOTAL，都在 :109-111 判定，没有单文件上限。第 1 分区（工具层）说得对，roadmap 的验收文案要跟着改。
- W2a-claudebinding 核对官方迁移文档（claude-api 技能 shared/model-migration.md:1622/1639/1647/1666）后：model id（claude-fable-5-1、claude-mythos-5-1、claude-opus-5-5）、beta 头 thinking-binding-controls-2026-08-01、prefix_mismatch_behavior 的取值（'error' 或 'drop_block'）、不带头时的「block_binding: Extra inputs are not permitted」、400 文案里的「bound to a different conversation」、Opus 5.5 上 disabled 与 budget_tokens 都返回 400，全部属实。需要补充两点。一是 drop_block 只对当次请求生效，并且会丢掉第一个不匹配块及其后所有 thinking 块，所以每个请求都必须带上（方案里已经带了）。二是文档写明 block_binding 对不做强制校验的模型同样可以接受、同一个请求体跨模型通用，因此精确的三模型白名单算保守做法，不是必需。另外，文档明确写了没有这套控制的平台和端点只能靠 strip-and-retry 恢复，这支持把「第三方端点剥掉 thinking 后重试一次」纳入 W2a，而不是只报错。
- W1b-delete 说「被删除打断的 run，runErr 记为『会话已删除』」，但漏掉了一个更普遍的问题：runAgentLoop 遇到失败是 yield error 事件然后正常返回（loop.ts:298/305/440/459/468/493/513/574），并不 throw，所以 index.ts:748-750 的 catch 对 provider 失败、「已取消」「所有模型均不可用」都不会触发，runErr 一直是 undefined，index.ts:1160 就把定时任务的 last_status 记成 'ok'，StageCron.vue:172 照样显示「上次 ok」。修正办法是在 IIFE 里记下最后一个 error 事件的 message 作为 runErr（见 missing）。
- MCP 分区说「mcp.servers.test 带字段的分支 :862-870，mkdtempSync 与 tmpdir 只有这里用」，已核对属实（index.ts:1/:3 的导入只在 :863 使用，rmSync 在 :1012 还有别处在用，不能一起删）。W2b-synchello 说 guardBusinessMethod 会透传 conn（remote/index.ts:252-256），conn.syncPeer 可以写到同一个对象上，也属实。

## missing
- 后端 RPC chat.sessions.applyAssistant 没有分区认领。W2b-welcome 的推荐方案和 mu6 的 WIRED 登记都依赖它。内容：拒绝有消息的会话和运行中的会话；把 assistantId、modelBinding、技能覆盖一起重置（setSessionOverride(null) 后再按快照写入）；广播 chat.sessions.changed；标题仍是默认值时改成助手名。另需 tests/session-apply-assistant.test.ts。要么并入 W2b-welcome 那一步，要么在它之前单独提交。
- W1b-delete 的渲染端配套没有分区认领：删除改成异步后最长要等 10 秒，超时会报错，而 NavRail.vue:54-57 的 onDelete 没有 try/catch，也没有「删除中」状态，报错会变成未处理的 rejection，界面看起来就是点了没反应。需要把错误显示在菜单里并加上进行中状态，同时加源码守卫并做 xvfb 目视。
- 新发现，属于报告 §4 B 组「界面撒谎」同一类：定时任务失败也记成 ok。loop 只 yield error 事件、不 throw，index.ts:1159-1162 的完成钩子拿到的 err 恒为 undefined，结果 StageCron 对失败的运行显示「上次 ok」。改动很小：在 index.ts:746 的广播循环里，遇到 event.kind==='error' 就记下 runErr=event.message。建议并入 W1b-delete，因为那一步本来就在改这段 IIFE。另加一例：FakeProvider 的 __fail__ 走 cron.runNow，断言 lastStatus 以 'error' 开头。
- MCP 的「即时生效」是假话，没有分区认领（MCP 分区的开放问题第 8 条）。manager.ts:114-152 只会去连还没连上的服务器；执行器也不看 store 里的 enabled。全局停用、删除或编辑一台已连上的服务器后，它的工具要等 10 分钟空闲驱逐才下表，而 SecMcp.vue:80 写着「改动即时生效，不用重启」。W1a-mcpedit 修好编辑之后，用户改了 env 会以为立刻生效。最小改法：执行器在调用前查 store.enabled；upsert 和 remove 的处理器通知 manager.forget(name)。建议并入 W1a-mcpedit 后端那一步；如果不修，就在 README 那一步把 :80 的文案改掉。
- 界面上的同类假话没有落到任何分区：TerminalPane.vue:63/:108 写「与 agent 共用同一个 shell」，但 terminal.ts:8 注释写明是独立实例；StageCron.vue:92 写「它不是后台服务」，但关窗后程序仍在托盘，定时任务照常触发；StageDevices.vue:87 写「会话与设置在两边同步」。发布工程分区把这几处推给了渲染分区，渲染分区没有接。应和 README 那一步一起改，做 xvfb 目视。
- 子进程环境变量没有收窄，roadmap 工具层那一行列了这个缺陷，但没有排进波次。生命周期分区说这「属于 W1b 的其它分区」，实际上没有分区认领。W1a-devdata 之后，DESKMINIS_DATA_DIR 等变量会经 shell.ts:63、terminal.ts:75、mcp/stdio.ts:179 传给所有子进程。agent 在 shell 里跑 DeskMinis 自己的 npm run dev 时，子进程会被导向正式数据根，dev 隔离就失效了（数据根锁能挡住写库，但体验会误导人）。建议在 killtree 那一步剥掉 DESKMINIS_* 这组变量。
- 报告 §4 B#17（系统提示每一步都重建，记忆块里带当日日志：index.ts:657/:665）被 roadmap 放到了 W5，没有进任何止血分区。但 W2a-claudebinding 之后，第三方端点对 2026-08-31 之后注册的账号，只要一次 memory_write 改了系统提示，后续请求就会 400，报出 thinkingBinding 中文错误，会话就此走不下去。至少要做两件事：把「第三方端点剥掉 thinking 块后重试一次」纳入 W2a（官方文档给出的恢复法）；CHANGELOG 的已知边界里写明。
- 报告 §4 B#16 只修了一部分：Fable 5、Opus 5、Sonnet 5、Opus 4.7/4.8 同样拒收 budget_tokens（claude-api 技能的思考表），「对默认开思考的模型显式关闭」也没做，这些都推到了 W4c。渲染端从不传 thinkingLevel，所以只有远端调用和桥接路径会触发。0.3.0 的 README 与 CHANGELOG 必须如实写出（发布工程分区的 README.md:16 改写已经覆盖了这条的对外表述），并在 W4c 的入口处登记。
- （低优先级，报告里「未复核」一栏）托盘菜单的「打开设置」「切换右栏」没有接上：preload/index.ts:17-27 暴露了 onMenuOpenSettings 和 onMenuToggleRight，但 src/renderer/src 里一处订阅都没有（已 grep 确认）；main/index.ts:77-78 仍然在 send。这两条托盘菜单点了没有反应，本身也是界面撒谎。W2b 可以顺手在 AppShell 订阅；不修的话，就在 README 或 CHANGELOG 的已知问题里写明。
- （低优先级）删除会话时不会删掉磁盘上的 sessions/<id>/ 各个桶。W1b-datagate 之后，这些残留目录会被判为「其它会话」，读写都要走卡，但文件一直留在数据根里。不属于 A/B 组，在 CHANGELOG 里写明即可。

## steps
- S0a 基线守卫与登记格式（不改行为） | items: W2b-depsfrozen：tests/deps-frozen.test.ts 做依赖快照，并附 diffDeps 纯函数, W2b-license：package.json:29 改为 Apache-2.0；package-lock.json 根元数据手改三处；electron-builder.yml 的 extraResources 追加 LICENSE 和 NOTICES, 修正 NOTICES：§1 Appica/AionUi、§2 报告数、§3 依赖；定下每个上游一节加一张表的登记格式，新建 pi-mono、ZCode 两节的空表, tests/license-consistency.test.ts 加双向绊线；变异自检的输出写进提交正文 | files: /home/user/Deskminis/deskminis/tests/deps-frozen.test.ts（新）, /home/user/Deskminis/deskminis/tests/license-consistency.test.ts（新）, /home/user/Deskminis/deskminis/package.json:29, /home/user/Deskminis/deskminis/package-lock.json:3,:9,:11, /home/user/Deskminis/deskminis/electron-builder.yml:19-23, /home/user/Deskminis/THIRD-PARTY-NOTICES.md, /home/user/Deskminis/docs/RELEASE.md §6 | deps: 
- S0b 请求体黄金快照（只加测试，先冻结再改 provider） | items: W2a-deepseek ⑤：tests/provider-body-golden.test.ts 用 sha256 硬编码表覆盖「非 v4 的 OpenAI 兼容模型 × 请求形态 × flags」，以及「Anthropic 非目标模型 × 官方/第三方 baseUrl」 | files: /home/user/Deskminis/deskminis/tests/provider-body-golden.test.ts（新） | deps: 
- S1 W1a file_edit 止血 | items: W1a-edit：indexOf+slice 拼接；拒收非 UTF-8；显式保留 BOM；CRLF 归一后做偏移映射；重叠出现也计入唯一性检查；拒绝空的 old_string, 抽出纯函数 edit-text.ts，文件头按 S0a 的格式写来源声明；在 NOTICES 的 pi-mono 表里追加一行 | files: /home/user/Deskminis/deskminis/src/minisd/tools/files.ts:102,:118-123, /home/user/Deskminis/deskminis/src/minisd/tools/edit-text.ts（新）, /home/user/Deskminis/deskminis/tests/file-edit-dollar.test.ts（新）, /home/user/Deskminis/THIRD-PARTY-NOTICES.md | deps: S0a 基线守卫与登记格式（不改行为）
- S2 W1a 技能 zip 上限 | items: W1a-zip：office/zip.ts 导出预算闸，单文件上限等于总量上限，行为不变；unzipToMemory 在 entryCount 阶段、inflate 之前、流式累计三处各设一道闸 | files: /home/user/Deskminis/deskminis/src/minisd/office/zip.ts:14-15,:105-118, /home/user/Deskminis/deskminis/src/minisd/skills/importer.ts:51-117, /home/user/Deskminis/deskminis/tests/skill-zip-bomb.test.ts（新） | deps: 
- S3 W1a MCP 配置损坏时拒写 | items: W1a-mcpcorrupt：读取时区分 ENOENT 和其它错误；剥掉 BOM；空文件当空配置；loadErrorKind 分为 read/parse/shape；upsert/remove/toggle/save 首行调用 assertWritable；list 只在出错时带 configErrorKind, SecMcp：删掉 :82-84 的重复横幅；configError 时隐藏添加按钮；开关改为 onToggle，失败时回滚 | files: /home/user/Deskminis/deskminis/src/minisd/mcp/config.ts:4,:109-135,:152,:186,:213,:218, /home/user/Deskminis/deskminis/src/minisd/index.ts:837, /home/user/Deskminis/deskminis/src/renderer/src/stores/chat.ts:77-84,:335-339, /home/user/Deskminis/deskminis/src/renderer/src/ui/settings/SecMcp.vue:82-95,:119, /home/user/Deskminis/deskminis/tests/mcp-config-corrupt.test.ts（新）, /home/user/Deskminis/deskminis/tests/market-install.test.ts:163-180, /home/user/Deskminis/deskminis/tests/renderer-mcp-settings.test.ts | deps: 
- S4 W1a MCP 外部修改不被覆盖 | items: W1a-mcpstale：记录 lastDiskText；写方法在 assertWritable 之前先调 syncFromDisk；mcp.servers.list 先 refresh；横幅文案改为「修好后回到这页即可」 | files: /home/user/Deskminis/deskminis/src/minisd/mcp/config.ts, /home/user/Deskminis/deskminis/src/minisd/index.ts:837, /home/user/Deskminis/deskminis/src/renderer/src/ui/settings/SecMcp.vue:89-91, /home/user/Deskminis/deskminis/tests/mcp-config-stale.test.ts（新） | deps: S3 W1a MCP 配置损坏时拒写
- S5 W1a MCP 编辑不丢字段（后端） | items: W1a-mcpedit：抽出 encodeEntry；compose 打补丁（null 表示删除、换传输类型时清掉旧族字段）；renameFrom 在原位改名；preview 不写盘, index.ts:862-870 的试连改用 preview，删掉 mkdtempSync 和 tmpdir 的导入；market/install.ts:495 的 env 为空时传 null, （建议并入）manager 在 upsert/remove 后 forget；执行器调用前查 enabled，让「即时生效」成真, （可选）W1a-mcplossy | files: /home/user/Deskminis/deskminis/src/minisd/mcp/config.ts:152-211, /home/user/Deskminis/deskminis/src/minisd/index.ts:1,:3,:838-870, /home/user/Deskminis/deskminis/src/minisd/market/install.ts:495, /home/user/Deskminis/deskminis/src/minisd/mcp/manager.ts（若并入）, /home/user/Deskminis/deskminis/tests/mcp-config-edit-preserve.test.ts（新）, /home/user/Deskminis/deskminis/tests/mcp-test-rpc.test.ts, /home/user/Deskminis/deskminis/tests/market-update.test.ts | deps: S4 W1a MCP 外部修改不被覆盖
- S6 W1a MCP 编辑表单（渲染端） | items: W1a-mcpedit-ui：lib/mcp/edit.ts 实现 argsToText、textToArgs、buildMcpUpsert；SecMcp 参数框改成 textarea，只提交改过的字段，改名走 renameFrom，新建时前端拦截同名, W1a-mcpguards：renderer-mcp-settings 新增两个 describe，只认调用形态；做 xvfb 目视 | files: /home/user/Deskminis/deskminis/src/renderer/src/lib/mcp/edit.ts（新）, /home/user/Deskminis/deskminis/src/renderer/src/ui/settings/SecMcp.vue:11-74,:141-145, /home/user/Deskminis/deskminis/tests/renderer-mcp-edit.test.ts（新）, /home/user/Deskminis/deskminis/tests/renderer-mcp-settings.test.ts | deps: S5 W1a MCP 编辑不丢字段（后端）
- S7 W1a 迁移守卫与启动致命错误通道 | items: W1a-dbguard：导出并 freeze MIGRATIONS；定义 DbNewerThanAppError；在打开 WAL 之前先判断库版本是否更新；runMigrations 用 db.transaction 包裹，失败时 close 再抛；用 sha256 钉住历史迁移, standalone 分支写 minisdFatal 行；新建 main/minisd-fatal.ts；主进程 stdout 识别 fatal 行，stderr 加 4KB 环形缓冲，按错误类型选 showMessageBox 或 showErrorBox | files: /home/user/Deskminis/deskminis/src/minisd/store/db.ts:4,:189-203, /home/user/Deskminis/deskminis/src/minisd/index.ts:1229-1234, /home/user/Deskminis/deskminis/src/main/minisd-fatal.ts（新）, /home/user/Deskminis/deskminis/src/main/index.ts:48-66,:222-228, /home/user/Deskminis/deskminis/tests/db-newer-than-app.test.ts（新）, /home/user/Deskminis/deskminis/tests/db-migrations-immutable.test.ts（新）, /home/user/Deskminis/deskminis/tests/minisd-fatal.test.ts（新） | deps: S5 W1a MCP 编辑不丢字段（后端）
- S8 W1a 开发态数据隔离（需用户点头） | items: W1a-devdata：新建 main/app-dirs.ts，resolveAppDirs 同时给出 logRoot 供 S25 使用；在模块顶层 setPath；fork 时下发 DATA_DIR、KEYRING_SERVICE、LOG_DIR；attachments:save 改用 dirs.dataRoot；KeyringVault 的服务名可注入, ipc-contract 的 electron 桩一次补齐 setPath、requestSingleInstanceLock、relaunch、exit；更新 e2e、dry-run 脚本注释和 driver README | files: /home/user/Deskminis/deskminis/src/main/app-dirs.ts（新）, /home/user/Deskminis/deskminis/src/minisd/paths.ts:9-13, /home/user/Deskminis/deskminis/src/main/index.ts:5,:14,:35,:200, /home/user/Deskminis/deskminis/src/minisd/store/provider-store.ts:38-42, /home/user/Deskminis/deskminis/src/minisd/index.ts:247, /home/user/Deskminis/deskminis/tests/ipc-contract.test.ts:16-19, /home/user/Deskminis/deskminis/tests/app-dirs.test.ts（新）, /home/user/Deskminis/deskminis/tests/keyring-service.test.ts（新）, /home/user/Deskminis/deskminis/tests/main-app-dirs-wiring.test.ts（新）, /home/user/Deskminis/deskminis/scripts/e2e-acceptance.mjs, /home/user/Deskminis/deskminis/scripts/e2e-m2b-acceptance.mjs, /home/user/Deskminis/deskminis/scripts/dry-run.mjs, /home/user/deskminis-docs/docs/handoff/driver/README.md | deps: S7 W1a 迁移守卫与启动致命错误通道
- S9 W1b 杀进程树与绝对路径启动 | items: W1b-killtree：新建 proc/win-exec.ts，提供 killTree、systemRoot、system32、powershellPath；shell 与终端改用绝对路径启动并加 windowsHide；interrupt、dispose、超时统一走 killTree；mcp/stdio 改为 re-export，cmd.exe 改绝对路径, ShellManager.dispose(sessionId) 一并加上，供 S12 调用, （建议）shell、终端、MCP 子进程的 env 剥掉 DESKMINIS_* 变量, mcp-stdio.test.ts 的 :217-231、:279-290 有意翻红并重指，在提交里逐条申报；NOTICES 的 pi-mono 表追加一行 | files: /home/user/Deskminis/deskminis/src/minisd/proc/win-exec.ts（新）, /home/user/Deskminis/deskminis/src/minisd/tools/shell.ts:1,:56-64,:151-178, /home/user/Deskminis/deskminis/src/minisd/terminal.ts:2,:53-110,:138, /home/user/Deskminis/deskminis/src/minisd/mcp/stdio.ts:89,:104-122,:179, /home/user/Deskminis/deskminis/src/minisd/bridge/handlers.ts:52-68（可选）, /home/user/Deskminis/deskminis/tests/shell-kill-tree.test.ts（新）, /home/user/Deskminis/deskminis/tests/mcp-stdio.test.ts, /home/user/Deskminis/THIRD-PARTY-NOTICES.md | deps: S0a 基线守卫与登记格式（不改行为）
- S10 W1b 数据根写入收窄 | items: W1b-datagate：新增 dataGate，带可注入的规范化与 realpath；硬拒名单包含核心数据、凭据文件以及保留锁文件名 minisd.lock(.recovery)；按白名单免审；应用配置、其它会话、其余路径走卡并附 note；读取规则同样生效（当前会话 offloads 保持免审）, PermissionRequest 加 note 字段；search 系工具跳过数据根子树；chat.ts 的 PendingPerm 加 note；PermCard 显示 note 行；e2e-m2c 注释同步 | files: /home/user/Deskminis/deskminis/src/minisd/tools/files.ts:17-49, /home/user/Deskminis/deskminis/src/minisd/tools/types.ts:16-21, /home/user/Deskminis/deskminis/src/minisd/paths.ts:4-7, /home/user/Deskminis/deskminis/src/minisd/tools/search.ts:74,:112-124, /home/user/Deskminis/deskminis/src/renderer/src/stores/chat.ts:10,:155-161, /home/user/Deskminis/deskminis/src/renderer/src/ui/PermCard.vue:22-27,:60-63, /home/user/Deskminis/deskminis/tests/data-root-write-gate.test.ts（新）, /home/user/Deskminis/deskminis/tests/renderer-permcard.test.ts, /home/user/Deskminis/deskminis/scripts/e2e-m2c-acceptance.mjs:4,:149 | deps: S1 W1a file_edit 止血, S8 W1a 开发态数据隔离（需用户点头）, S3 W1a MCP 配置损坏时拒写
- S11 W1b 单实例锁与数据根锁 | items: W1b-single：新建 store/data-root-lock.ts（改编自 ZCode，登记在 NOTICES 的 ZCode 表）；startMinisd 改成外壳，调用改名后的 assembleMinisd（函数体不重缩进）；close 最后一步释放锁；fatal 行加 DATA_ROOT_LOCKED, 主进程：顶层 requestSingleInstanceLock，拿不到就 quit，whenReady 首行早退；second-instance 时 restore、show、focus，退出流程中忽略；新增模块级 mainWindow；e2e 脚本注释改为「先退出应用或用临时根」 | files: /home/user/Deskminis/deskminis/src/minisd/store/data-root-lock.ts（新）, /home/user/Deskminis/deskminis/src/minisd/index.ts:231-235,:1206-1216,:1229-1234, /home/user/Deskminis/deskminis/src/main/minisd-fatal.ts, /home/user/Deskminis/deskminis/src/main/index.ts:8,:14 之后,:206,:216, /home/user/Deskminis/deskminis/tests/data-root-lock.test.ts（新）, /home/user/Deskminis/deskminis/tests/data-root-lock-minisd.test.ts（新）, /home/user/Deskminis/deskminis/tests/single-instance.test.ts（新）, /home/user/Deskminis/deskminis/scripts/e2e-acceptance.mjs, /home/user/Deskminis/deskminis/scripts/e2e-m2b-acceptance.mjs, /home/user/Deskminis/THIRD-PARTY-NOTICES.md | deps: S7 W1a 迁移守卫与启动致命错误通道, S8 W1a 开发态数据隔离（需用户点头）, S10 W1b 数据根写入收窄
- S12 W1b 删除运行中会话，并修定时任务假 ok | items: W1b-delete：新增 runs Map 与 deferred；denyPendingPerms 与 stopRun 由 delete 和 close 共用；delete 改成 async，按 deny → abort → 等收尾（10s）→ 终端和 shell dispose → deleteSession → 广播的顺序执行；chat.prompt 在 ensureForRun 之后复查 aborted, （新增）IIFE 记下最后一个 error 事件作为 runErr，定时任务失败不再记为 ok, （新增）NavRail.vue 的 onDelete 加 try/catch 和删除中状态，配源码守卫, 红测的权限触发改用 web_fetch，或 datagate 之后的 /var/minis/skills 路径，不用 POSIX 绝对路径 | files: /home/user/Deskminis/deskminis/src/minisd/index.ts:281,:404-408,:517-522,:673-758, /home/user/Deskminis/deskminis/src/renderer/src/ui/NavRail.vue:54-57, /home/user/Deskminis/deskminis/tests/session-delete-running.test.ts（新）, /home/user/Deskminis/deskminis/tests/cron-run-status.test.ts（新，或并入上一文件） | deps: S9 W1b 杀进程树与绝对路径启动, S10 W1b 数据根写入收窄, S11 W1b 单实例锁与数据根锁
- S13 W1b 优雅退出 | items: W1b-quit：主进程新增 stopMinisdGracefully（postMessage shutdown，5s 后 kill 兜底，保证幂等）；before-quit 先 preventDefault，隐藏窗口、销毁托盘，停完再 quit；update-downloaded 里先 await 停止再 quitAndInstall，并让 `quitAndInstall();` 与 `}` 保持同一行；加常驻 exit 监听, minisd：close 重写为幂等、graceMs、closing 闸；standalone 分支通过 parentPort 接收 shutdown, tray-lifecycle.test.ts:33-49 有意重指，在提交里逐条申报 | files: /home/user/Deskminis/deskminis/src/main/index.ts:17,:66,:131-148,:226,:230, /home/user/Deskminis/deskminis/src/minisd/index.ts:582,:1131,:1206-1228, /home/user/Deskminis/deskminis/tests/tray-lifecycle.test.ts:33-49, /home/user/Deskminis/deskminis/tests/main-shutdown-wiring.test.ts（新）, /home/user/Deskminis/deskminis/tests/shutdown-partial-reply.test.ts（新） | deps: S12 W1b 删除运行中会话，并修定时任务假 ok
- S14 W2a 压缩止血 | items: W2a-compact：取材改为「旧摘要 + 增量」；压平成单条 user 文本；逐项截断；按窗口预算让锚点后退；为空、截断或拒绝时抛 CompactRejectedError；loop 发 compactFailed 并设 compactDisabled；STOP_MAP 补 model_context_window_exceeded；auto-title 输出上限 64→2048, context-policy 导出 estimateTextTokens、windowOf、FALLBACK_WINDOW, 渲染端：chat.ts:106 在联合类型末尾追加 'compactFailed' 并加分支；copy.ts 与 EventNotes 补上对应项, compact.test.ts:92/:117 与 auto-title.test.ts:8-11 有意翻红重指；复跑 agent-loop.test.ts:888-959，确认耗时 | files: /home/user/Deskminis/deskminis/src/minisd/agent/compact.ts:48-137, /home/user/Deskminis/deskminis/src/minisd/agent/context-policy.ts:6,:17,:24-35, /home/user/Deskminis/deskminis/src/minisd/agent/loop.ts:15-27,:321-342, /home/user/Deskminis/deskminis/src/minisd/providers/anthropic.ts:93, /home/user/Deskminis/deskminis/src/minisd/agent/auto-title.ts:24, /home/user/Deskminis/deskminis/src/renderer/src/stores/chat.ts:106,:566, /home/user/Deskminis/deskminis/src/renderer/src/lib/eventnote/copy.ts:31-40, /home/user/Deskminis/deskminis/src/renderer/src/ui/EventNotes.vue:16-23, /home/user/Deskminis/deskminis/tests/compact-bounded.test.ts（新）, /home/user/Deskminis/deskminis/tests/compact.test.ts:92,:117, /home/user/Deskminis/deskminis/tests/auto-title.test.ts:8-11, /home/user/Deskminis/deskminis/tests/anthropic.test.ts | deps: S10 W1b 数据根写入收窄
- S15 W2a 溢出分类（引擎，定契约） | items: W2a-overflow：新建 providers/overflow.ts，移植 pi 的正则表，按 NOTICES 格式写文件头；ProviderError.code 的联合类型一次定为 'contextOverflow'|'thinkingBinding'；loop 在 :427 之后拦截：只降级到窗口更大的槽位，找不到就发 {kind:'error',code:'contextFull',message,relayDraft≤8000字}，fallback 事件带 cause:'contextOverflow', 契约用测试钉住：provider-overflow.test.ts 断言 code 与 relayDraft 字段；NOTICES 的 pi-mono 表追加一行 | files: /home/user/Deskminis/deskminis/src/minisd/providers/overflow.ts（新）, /home/user/Deskminis/deskminis/src/minisd/providers/types.ts:17-29, /home/user/Deskminis/deskminis/src/minisd/agent/loop.ts:23,:27,:427-462, /home/user/Deskminis/deskminis/tests/provider-overflow.test.ts（新）, /home/user/Deskminis/THIRD-PARTY-NOTICES.md | deps: S14 W2a 压缩止血
- S16 W2a 最新 Claude 止血 | items: W2a-claudebinding：官方端点上的三个精确 model id 加 beta 头和 adaptive+block_binding(drop_block)；这三个模型一律不发 budget_tokens/disabled；「bound to a different conversation」的 400 归为 thinkingBinding，不可降级，中文说明放在最前；loop 拦截该错误, （建议纳入）第三方端点命中该 400 时，剥掉全部 thinking 块后重试一次, 以 S0b 的黄金快照为回归钉 | files: /home/user/Deskminis/deskminis/src/minisd/providers/anthropic.ts:1-47,:100-112, /home/user/Deskminis/deskminis/src/minisd/agent/loop.ts:427 之后, /home/user/Deskminis/deskminis/tests/anthropic-binding-controls.test.ts（新） | deps: S0b 请求体黄金快照（只加测试，先冻结再改 provider）, S15 W2a 溢出分类（引擎，定契约）
- S17 W2a DeepSeek V4 回传 reasoning_content | items: W2a-deepseek：AgentMessage 加可选字段 reasoningContent；新建 agent-message.ts，由 toAgentMessages 和 buildEffectiveHistory 共用；prune.ts:41 改成 {...m, parts}；openai.ts 对 deepseek-v4 族的每条 assistant 回填 reasoning_content | files: /home/user/Deskminis/deskminis/src/shared/types.ts:76, /home/user/Deskminis/deskminis/src/minisd/agent/agent-message.ts（新）, /home/user/Deskminis/deskminis/src/minisd/agent/loop.ts:66-77, /home/user/Deskminis/deskminis/src/minisd/agent/compact.ts:105-137, /home/user/Deskminis/deskminis/src/minisd/agent/prune.ts:41, /home/user/Deskminis/deskminis/src/minisd/providers/openai.ts:19-33, /home/user/Deskminis/deskminis/src/minisd/agent/context-policy.ts:12-14, /home/user/Deskminis/deskminis/tests/openai-deepseek-reasoning-echo.test.ts（新） | deps: S0b 请求体黄金快照（只加测试，先冻结再改 provider）, S16 W2a 最新 Claude 止血
- S18 W2a 卸载与修剪的桩文案如实 | items: W2a-honest：新增 readBackOf 和 clampReadBack（上限 5 万字符）；loop.ts:557-562 读回时不再二次卸载；超过 READBACK_MAX 时修改桩的第三行；prune 桩改为如实说明, 删掉红测 ②（宿主绝对路径在 Linux 上跑不通），改成 readBackOf 的单元测试 | files: /home/user/Deskminis/deskminis/src/minisd/agent/offload.ts:1-36, /home/user/Deskminis/deskminis/src/minisd/agent/loop.ts:557-562, /home/user/Deskminis/deskminis/src/minisd/agent/prune.ts:10-22,:39, /home/user/Deskminis/deskminis/tests/offload-roundtrip.test.ts（新）, /home/user/Deskminis/deskminis/tests/prune-stub-honest.test.ts（新） | deps: S10 W1b 数据根写入收窄, S17 W2a DeepSeek V4 回传 reasoning_content
- S19a W2b 按会话跟踪运行状态 | items: W2b-runset：新增 runningSessions 与 trackRun（在按 activeId 过滤之前调用）；open() 只替换 running 那一句；send 维护集合；加 midRun 占位 | files: /home/user/Deskminis/deskminis/src/renderer/src/stores/chat.ts:95,:134-143,:397,:447,:456,:519-537, /home/user/Deskminis/deskminis/src/renderer/src/ui/StageChat.vue:197-209, /home/user/Deskminis/deskminis/tests/renderer-run-tracking.test.ts（新） | deps: S14 W2a 压缩止血
- S19b W2b 权限卡按会话区分 | items: W2b-permscope：PendingPerm 加 sessionId；新建 lib/perm/scope.ts；StageChat 只渲染当前会话的卡，并提示「另有 N 个会话在等你批准」；NavRail 行上加盾牌标；TaskPanel 与 WorkspacePanel 改用当前会话的计数；超时留条只在卡属于当前会话时写入, renderer-chat-capabilities.test.ts:23 有意重指 | files: /home/user/Deskminis/deskminis/src/renderer/src/stores/chat.ts:10,:155-170, /home/user/Deskminis/deskminis/src/renderer/src/lib/perm/scope.ts（新）, /home/user/Deskminis/deskminis/src/renderer/src/ui/StageChat.vue:158,:216, /home/user/Deskminis/deskminis/src/renderer/src/ui/NavRail.vue:20-21,:119-122,:284-289, /home/user/Deskminis/deskminis/src/renderer/src/ui/TaskPanel.vue:62-66, /home/user/Deskminis/deskminis/src/renderer/src/ui/WorkspacePanel.vue:100, /home/user/Deskminis/deskminis/tests/perm-session-scope.test.ts（新）, /home/user/Deskminis/deskminis/tests/renderer-chat-capabilities.test.ts:23 | deps: S10 W1b 数据根写入收窄, S19a W2b 按会话跟踪运行状态, S12 W1b 删除运行中会话，并修定时任务假 ok
- S20 W2b 重启通道与断线横幅 | items: W2b-relaunch：preload 暴露 relaunchApp；主进程 app:relaunch 校验 sender 是 mainWindow，然后 relaunch 再 quit（便携版带上 PORTABLE_EXECUTABLE_FILE）, W2b-disconnect：rpc.ts 加 onclose 与 lost 标志，pending 统一 reject，断线后的新调用立即失败，提供 onLost；store 在 connect 之前注册 onLost，markConnectionLost 清空 runningSessions 与 pendingPerms；TopBar 显示横幅；canSend 加断线条件, 两处测试桩补 onLost；xvfb 场景的权限卡改用 web_fetch 触发 | files: /home/user/Deskminis/deskminis/src/preload/index.ts:31, /home/user/Deskminis/deskminis/src/main/index.ts:177 旁, /home/user/Deskminis/deskminis/src/renderer/src/rpc.ts:4-68, /home/user/Deskminis/deskminis/src/renderer/src/stores/chat.ts:94-95,:129-130, /home/user/Deskminis/deskminis/src/renderer/src/ui/TopBar.vue:27-78, /home/user/Deskminis/deskminis/src/renderer/src/ui/Composer.vue:33, /home/user/Deskminis/deskminis/tests/app-relaunch.test.ts（新）, /home/user/Deskminis/deskminis/tests/rpc-disconnect.test.ts（新）, /home/user/Deskminis/deskminis/tests/renderer-disconnect.test.ts（新）, /home/user/Deskminis/deskminis/tests/renderer-permtier.test.ts:16, /home/user/Deskminis/deskminis/tests/renderer-model-groups.test.ts:15 | deps: S13 W1b 优雅退出, S19b W2b 权限卡按会话区分
- S21 W2a 溢出的界面部分（按 S15 契约） | items: W2a-overflow UI：chat.ts 在 :528 之前加 contextFull 分支（retryable:false，relay:true）；relayDraft 定为 {sessionId,text}；fallback 分支按 cause 设 short；relayToNewSession 继承助手、模型绑定和工作区, EventNotes 加接力按钮，shortOf 优先用 n.short；Composer 在 setup 里用 takeRelay 消费，不用 watch；copy.ts 先按 code 选文案，并在 5xx 正则之前识别溢出, 两份渲染守卫合并为 tests/renderer-overflow-relay.test.ts | files: /home/user/Deskminis/deskminis/src/renderer/src/stores/chat.ts:50,:106,:528-548, /home/user/Deskminis/deskminis/src/renderer/src/ui/EventNotes.vue:24-27,:39,:62-67, /home/user/Deskminis/deskminis/src/renderer/src/ui/Composer.vue:259-260, /home/user/Deskminis/deskminis/src/renderer/src/lib/eventnote/copy.ts:13-40, /home/user/Deskminis/deskminis/tests/renderer-overflow-relay.test.ts（新）, /home/user/Deskminis/deskminis/tests/renderer-eventnote-copy.test.ts | deps: S15 W2a 溢出分类（引擎，定契约）, S20 W2b 重启通道与断线横幅
- S22 W2b 欢迎页选助手（含后端 RPC） | items: （补认领）minisd 新增 chat.sessions.applyAssistant，并补测试 session-apply-assistant, W2b-welcome：open() 从会话已绑的助手初始化 welcomeAssistantId；新增 applyAssistantToSession 与 ensureSession；新建 lib/welcome/assistant.ts；Composer 的 send 加 else 分支，saveImages 改用 ensureSession，effectiveBinding 改走 previewBinding；WorkspacePanel 改调 ensureSession, mu6 的 WIRED 登记；renderer-model-groups.test.ts:178 重指；「不再含 newSession」的守卫只限定在 saveImages 内 | files: /home/user/Deskminis/deskminis/src/minisd/index.ts:467 旁, /home/user/Deskminis/deskminis/src/renderer/src/stores/chat.ts:224-228,:401, /home/user/Deskminis/deskminis/src/renderer/src/lib/welcome/assistant.ts（新）, /home/user/Deskminis/deskminis/src/renderer/src/ui/Composer.vue:43-46,:181-185,:225-234, /home/user/Deskminis/deskminis/src/renderer/src/ui/WorkspacePanel.vue:34-36, /home/user/Deskminis/deskminis/tests/session-apply-assistant.test.ts（新）, /home/user/Deskminis/deskminis/tests/renderer-welcome-assistant.test.ts（新）, /home/user/Deskminis/deskminis/tests/mu6-capability-wiring.test.ts:62-82, /home/user/Deskminis/deskminis/tests/renderer-model-groups.test.ts:178 | deps: S21 W2a 溢出的界面部分（按 S15 契约）, S13 W1b 优雅退出
- S23 W2b ModelBar 标明默认模型 | items: W2b-modelbar：binding.ts 的 default 分支 label 前加「默认 · 」；ModelBar 加「默认模型」标签和 title，改正注释里的假话, models-binding.test.ts:41-48 有意重指 | files: /home/user/Deskminis/deskminis/src/renderer/src/lib/models/binding.ts:64-67, /home/user/Deskminis/deskminis/src/renderer/src/ui/ModelBar.vue:1-9,:37-49,:71-77, /home/user/Deskminis/deskminis/tests/models-binding.test.ts:41-48, /home/user/Deskminis/deskminis/tests/renderer-modelbar-default.test.ts（新） | deps: 
- S24 W2b 窗口导航守卫与权限白名单 | items: W2b-guards：新建 main/nav-guard.ts；setWindowOpenHandler 一律 deny，http/https/mailto 交给 openExternal；will-navigate 拦截非本应用 URL；权限请求与检查只放行 clipboard-sanitized-write, ipc-contract 桩补 shell 和 session | files: /home/user/Deskminis/deskminis/src/main/nav-guard.ts（新）, /home/user/Deskminis/deskminis/src/main/index.ts:1,:85-96,:206, /home/user/Deskminis/deskminis/tests/main-window-guard.test.ts（新）, /home/user/Deskminis/deskminis/tests/ipc-contract.test.ts | deps: S20 W2b 重启通道与断线横幅
- S25 W2b 本地崩溃记录与按天日志 | items: W2b-crashlog：新建 diag/crash-log.ts（最多 5 条、保留 7 天、tmp 加 rename 原子写）和 diag/daily-log.ts；主进程在 whenReady 里安装钩子，minisd 只在 standalone 分支安装；stdout/stderr 落到按天日志；复用 S13 的 exit 监听和 S7 的 stderr 环形缓冲；NOTICES 的 pi-mono 表追加一行 | files: /home/user/Deskminis/deskminis/src/minisd/diag/crash-log.ts（新）, /home/user/Deskminis/deskminis/src/minisd/diag/daily-log.ts（新）, /home/user/Deskminis/deskminis/src/main/index.ts:60,:65,:66 之后,:206, /home/user/Deskminis/deskminis/src/minisd/index.ts:1220, /home/user/Deskminis/deskminis/tests/crash-log.test.ts（新）, /home/user/Deskminis/deskminis/tests/daily-log.test.ts（新）, /home/user/Deskminis/deskminis/tests/crash-log-wiring.test.ts（新）, /home/user/Deskminis/THIRD-PARTY-NOTICES.md | deps: S24 W2b 窗口导航守卫与权限白名单, S22 W2b 欢迎页选助手（含后端 RPC）
- S26 W2b sync.hello 带协议版本 | items: W2b-synchello：wire.ts 新增 parseSyncHello 和 peerHasCap；应答端鉴权通过后才把对端信息记到 conn.syncPeer；发起端发送 protocolVersion 2 和空 caps，并提供 peerInfo；MAC 输入不变 | files: /home/user/Deskminis/deskminis/src/minisd/sync/wire.ts, /home/user/Deskminis/deskminis/src/minisd/sync/rpc.ts:72-85, /home/user/Deskminis/deskminis/src/minisd/sync/outbound-client.ts:5-6,:39-54,:125-136,:211-239, /home/user/Deskminis/deskminis/src/minisd/rpc/server.ts:7, /home/user/Deskminis/deskminis/tests/sync-hello-version.test.ts（新） | deps: 
- S27 W2b 更新源与错误文案（仓库名待用户拍板） | items: W2b-update：electron-builder.yml 的 repo 改为公开的发布仓库；新建 main/update-status.ts，describeUpdateError 输出中文说明；便携版直接返回 portable 状态；SecAbout 删掉私仓 404 那句，补上 portable 文案, （可选）托盘「检查更新…」手动检查后给出结果反馈 | files: /home/user/Deskminis/deskminis/electron-builder.yml:51-62, /home/user/Deskminis/deskminis/src/main/update-status.ts（新）, /home/user/Deskminis/deskminis/src/main/index.ts:79,:146-162, /home/user/Deskminis/deskminis/src/renderer/src/ui/settings/SecAbout.vue:16-26,:76,:87, /home/user/Deskminis/deskminis/tests/auto-update.test.ts, /home/user/Deskminis/deskminis/tests/update-error-text.test.ts（新） | deps: S25 W2b 本地崩溃记录与按天日志, S0a 基线守卫与登记格式（不改行为）
- S28 W2b 发布校验脚本 | items: W2b-verifyrelease：新建 scripts/verify-release.mjs（零依赖，含 YAML 最小解析、流式 sha512、blockmap 的 gzip 魔数、便携版检查，app-update.yml 与 publish 一致，resources 里有 LICENSE.txt）；package.json 加 verify:release；RELEASE.md 加校验步骤，要求上传四个资产 | files: /home/user/Deskminis/deskminis/scripts/verify-release.mjs（新）, /home/user/Deskminis/deskminis/package.json:6-26, /home/user/Deskminis/deskminis/tests/verify-release.test.ts（新）, /home/user/Deskminis/deskminis/tests/m5-packaging.test.ts, /home/user/Deskminis/docs/RELEASE.md:19,§2,§4 | deps: S27 W2b 更新源与错误文案（仓库名待用户拍板）
- S29 W2b 对外文档与界面文案逐行核对（最后一步） | items: W2b-readme：README 能力表逐行核对，包括 :28 的 MCP 行，提交正文附上核对清单；CHANGELOG 0.3.0 节补充止血项和已知边界（B#16 余项、B#17、第三方端点、会话目录不删、托盘死通道）；RELEASE.md 同步, 界面上的同类假话：TerminalPane.vue:63/:108、StageCron.vue:92、StageDevices.vue:87，以及 manager 未修时的 SecMcp.vue:80；做 xvfb 目视, （可选）tests/readme-claims.test.ts | files: /home/user/Deskminis/README.md, /home/user/Deskminis/CHANGELOG.md, /home/user/Deskminis/docs/RELEASE.md, /home/user/Deskminis/deskminis/src/renderer/src/ui/TerminalPane.vue:63,:108, /home/user/Deskminis/deskminis/src/renderer/src/ui/StageCron.vue:92, /home/user/Deskminis/deskminis/src/renderer/src/ui/StageDevices.vue:87, /home/user/Deskminis/deskminis/src/renderer/src/ui/settings/SecMcp.vue:80, /home/user/Deskminis/deskminis/tests/readme-claims.test.ts（新，可选） | deps: S2 W1a 技能 zip 上限, S6 W1a MCP 编辑表单（渲染端）, S18 W2a 卸载与修剪的桩文案如实, S22 W2b 欢迎页选助手（含后端 RPC）, S23 W2b ModelBar 标明默认模型, S26 W2b sync.hello 带协议版本, S28 W2b 发布校验脚本