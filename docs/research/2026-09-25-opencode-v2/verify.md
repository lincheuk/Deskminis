# 附录 D：对抗式核验

## 部件建议核验

| 部件 | 出自 OpenCode | 结论 | 最终建议 | 理由 | 与止血冲突 |
|---|---|---|---|---|---|
| 标题栏窗口形制（无边框 + 系统绘制窗控 titleBarOverlay + 右侧 146px 让位） | 是 | 需修正 | simplify | 出处成立，但证据是间接的：docs 提交 5c6e4f5（2026-07-27 00:04，说明写「自绘标题栏规格 + OpenCode 架构研究」）在同一次提交里新增了 ui-design.md:109-128（§4.0）和 opencode-0.md。参数与 opencode-0.md:9 所记的 OpenCode windows.ts 逐项一致；§4.0 正文没有写 OpenCode 的名字。代码落在 src/main/index.ts:131-133 和 TopBar.vue:2-4、:58；只借了思路，没有复制代码。THIRD-PARTY-NOTICES.md §5「仅借思路」（:113 起）也没列 OpenCode，可以补一条诚实登记（不是许可义务）。  需修正两处： 1. 「三家基准都用 titleBarOverlay」不成立。AionUi 在 packages/desktop/src/index.ts:488-499 里，Windows 下只设 frame:false，窗控是自绘的 components/layout/WindowControls.tsx，全仓 grep 不到 titleBarOverlay。真正用 overlay 的是 ZCode（desktopWindowButtonPosition.ts:90-110）和 OpenCode。保留 overlay 的理由应改成「已落地、ZCode 同做法、自绘窗控成本高」。 2. 注释与实现相悖：index.ts:129-130 注释写「符号色随明暗」，:133 实际写死 '#808080'；TopBar.vue:2-3 注释写留 140px，:58 样式是 146px。  简化方向本身成立：用 setTitleBarOverlay 让窗控随主题和缩放同步，经 IPC 把安全区宽度推给渲染端，即 roadmap.md:247 的 W9c。照搬 ZCode 代码要在 NOTICES §4 的改编表里登记（该表现在为空）。 | W2b-3 正在 TopBar 下方加断线横幅，并把 TopBar 改成多根组件（hemostasis/renderer.md 的 W2b-disconnect【TopBar】段）；main/index.ts 同时被 W2b-3 的 app:relaunch 和 W2b-6 的导航守卫改动。排在 W3 之后；窗控只能在 Windows 真机目视验证。 |
| TopBar ☰「菜单」钮（实际只切换明暗、不落盘） | 否 | 成立 | cut | 逐条核实： - TopBar.vue:33-35 按钮 title 是「菜单」；AppShell.vue:63-67 的 toggleTheme 只翻转 dataset.theme，:77 绑定 @menu； - SecLook.vue:16-27 只在外观页 onMounted 时读 localStorage；main.ts 与 index.html 都没有启动时读回。 报告 §4「核验者报告、主会话未复核」里的「启动时不恢复已保存的主题」这次坐实。  删 ☰ 必须同批把 SecLook 的 apply 抽成纯函数、在启动时调用，否则用户失去一键切换，保存的主题照样不生效。  出处与 OpenCode 无关：ui-design §4.0 规划了自绘菜单，I6 收成单个 ☰，T 波只剩切主题（renderer-assistants.test.ts:94 的退场注释有记）。 | W2b-3 正在改 TopBar。hemostasis/lifecycle.md:9、:377 指出主题存在 userData 下的 localStorage，与 W1a-9 的开发态数据隔离相关，启动读回要一起验证。归入 W9c「已保存的主题在首帧生效」，W3 之后做。 |
| 底部终端抽屉 TerminalPane + TopBar 终端钮 + minisd terminal.* / TerminalManager | 否 | 需修正 | cut | 出处核实不是 OpenCode：design.md:346 的 §7（提交 bcbdf55，2026-07-26 17:49）早于 OpenCode 研读，PROJECT_NOTES.md:33 也有记。「界面说假话」核实：TerminalPane.vue:63、:108，README.md:31，CHANGELOG.md:33，docs/RELEASE.md:47 都说与 agent 共用 shell；实际 minisd/terminal.ts:10 自称独立实例，index.ts:337 的 TerminalManager 与 :341 的 ShellManager 是两套。这条已列入 W2b-11（release.md:269、:381）。  需修正四点： 1. 受影响测试漏了三个整文件级的：    - shell-kill-tree.test.ts:19 在顶层 import TerminalSession/TerminalManager。这是 W1b-1 刚合入的进程树守卫，删 terminal.ts 会让 PersistentShell、MCP、runPowerShell 的全部用例一起红；    - workspace-picker.test.ts:32 在模块级读 src/minisd/terminal.ts，整文件红；    - renderer-content-form.test.ts:17 在模块级读 TerminalPane.vue，4 例全红，不只终端那 2 例，用户气泡和工具组活动指示两条形态裁定也会连坐。    另外还有 tokens-mu3-appica.test.ts 例 9（:252），以及 license-consistency 对 NOTICES.md:173 @xterm 行与锁文件的对账。 2. 这是 V 波刚补回的能力（shell-capability-parity.md:33「V4 一整条能力不可达」）。PROJECT_NOTES.md:397 把「终端面板」列为已验证需求信号；m2d 计划 :40 的决策 #8 还有「在终端里手调桥命令」的场景。砍它属于能力退场，需要用户拍板，并在 CHANGELOG 写明「移除」：0.3.0 的 CHANGELOG.md:33 正把它当新功能宣传。 3. UI 读者说「改走命令面板或设置-高级开关」：DeskMinis 没有命令面板，设置里也没有高级分区（ui/settings/ 只有 8 个 Sec*）。那是新功能，不能算降级。 4. 分两步做：    - 先删界面入口：TopBar.vue:42-44，AppShell.vue:24、:31、:73、:76、:102，以及 TerminalPane.vue；    - 再单独一步删 terminal.ts 和 index.ts:337、:523、:964-965、:1235。shell-kill-tree 的 :220-285 与 workspace-picker 的终端断言原位退场、留理由；linux-baseline-failures.txt:48-52 删 5 行；@xterm 依赖要用户在本机点头改锁。 | 与 W2b-11 同改 TerminalPane.vue:63、:108 和 README:31，应由 W2b-11 先订正文案，发版后再删。RELEASE.md:47 的 W3 真机冒烟清单里有终端这一步。W1b-1 刚给终端加了进程树回收（shell-kill-tree :220-285）。发版前删等于推翻刚合入的止血成果，也违反 roadmap.md:311，所以 0.3.0 前不动。 |
| 任务面板 TaskPanel（上下文水位 + 降级/压缩/卸载/待批准 + 上轮结束原因） | 否 | 需修正 | replace | 核实： - TaskPanel.vue:16-18 每 15 秒轮询； - :34-39 的 STOP_TEXT 里，「达到单轮输出上限——回答可能被截断」在整个渲染层只出现这一处（chat.ts:527 只存 lastStopReason）； - :70-95 三张卡与 EventNotes 重复； - store 在 chat.ts:529、551、558、567、573 已经按事件调 fetchContextInfo。 「mu6 抓不到」的判断成立：mu6-capability-wiring.test.ts:144-150 只查 action，而 fetchContextInfo 在 store 内有 this. 调用，三个 state 字段也不是 action。  需修正四点： 1. renderer-tasks-panel.test.ts:13 在模块级读 TaskPanel.vue，删文件会让整份 6 例一起红，包括 S1-S4 这几条只锚 chat.ts 的 M2d 回归锚。要先把 S1-S4 挪到别的文件再删面板，不能「逐条退场」。S5（:63-65）还锚着 WorkspacePanel 的 tab === 'tasks' 和 import TaskPanel。 2. W2b-2 要新建 perm-session-scope.test.ts，其源码守卫会读 TaskPanel 和 WorkspacePanel（renderer.md W2b-2 先红段），删面板时要同步它。 3. 替代用的上下文环，数值要等 W5b 的真实用量。现在的 contextInfo 是后端按 buildEffectiveHistory 估出来的。AionUi ContextUsageIndicator.tsx:16-21 明确「不拿猜的分母给百分比」，与 W9c「依赖 W5b」（roadmap.md:247）一致。 4. 停止原因进 EventNotes，要追加在 chat.ts:108 联合类型的末尾。W2a-1 刚在那里追加 compactFailed，W2a-6 还要加 relay/short 字段（cross.md:45），会撞车。 | W2b-2 正在改 TaskPanel.vue:62-66 和 WorkspacePanel.vue:100；W2a 正在改 chat.ts、EventNotes.vue、lib/eventnote/copy.ts、Composer.vue；roadmap.md:311 禁止 0.3.0 前加新功能。W3 之后与 W9c 合做。 |
| 右栏 WorkspacePanel 三 tab 收成「文件 / 改动」两 tab | 否 | 需修正 | simplify | 方向成立：AionUi ExplorerContainer.tsx:512-548 只有 files/changes 两个 tab。WorkspacePanel.vue 文件头 :2-4 本来就写「两个 tab」，与现在的三 tab 自相矛盾。  位置修正：TaskPanel 的 v-else 在 :171，不是 prov 写的 :169。  漏列一个守卫：renderer-tasks-panel.test.ts 的 S5（:63-65）锚着 import TaskPanel 和 tab === 'tasks'。  任务 tab 上的橙点（:100）是右栏唯一的「等你批准」提示。删之前要确认 W2b-2 已落地两样：NavRail 的 .swait，和 StageChat 的「另有 N 个会话在等你批准」提示行。  必须与 TaskPanel 替换放在同一提交。 | W2b-2 改 :100，W2b-4 改 :34-36（删本地 ensureSession）。W3 之后做。 |
| 右栏默认展开（AppShell wsOpen=ref(true)，注释却写默认收起） | 否 | 需修正 | simplify | 相悖核实：AppShell.vue:6 写「Aside 默认收起」，:28 是 wsOpen = ref(true)；ui-rebuild-design.md:136 也写默认收起。没有守卫钉这个值。  UI 读者建议「按渐进披露改成默认收起」，这里需要修正：T6b 之后，工作区绑定入口只在右栏「文件」tab 里（WorkspacePanel.vue:108 的 v-if tab === 'files'）。默认收起会把 mu6 WIRED 的 setWorkspace/pickWorkspaceFolder 藏到二级；mu6 只查调用是否存在，不会红，这正是 T 波「换壳丢入口」的同型风险。改动清单也是 cowork 的主入口。  建议： - 先改注释对齐现实； - 默认收起与「首个产出物出现时自动展开」一起设计，放在 W8a 之后； - 「文件 tab 改成改动优先」同理，要先把绑定行挪出文件 tab。 | W2b-11 可能在 AppShell 订阅托盘「切换右栏」IPC（cross.md:66），会碰到同一个 wsOpen。W3 之后做。 |
| 改动清单（WorkspacePanel「改动」tab + lib/artifacts/collect） | 否 | 成立 | keep | collect.ts:2、:40-42 只认 file_write、file_edit、office_write，核实。出处不是 OpenCode：OpenCode 的对应物是 git 差分审阅（review-panel-v2.tsx），DeskMinis 没有照做。  shell 改动看不见的问题归 W8b。把「改动」改名为「产出」是可选项，守卫只锚 tab === 'changes' 和 collectArtifacts 调用形态。 | 无，W2b 不改这段。 |
| 文件树 + 工作区绑定（「文件」tab + UiFileTree） | 否 | 成立 | keep | 与 AionUi explorer 同构。绑定行在 WorkspacePanel.vue:108-145，是 mu6 WIRED 三条工作区能力唯一的界面入口，不能跟着任何 tab 调整被挪丢。 | W2b-4 改 WorkspacePanel.vue:34-36 的 ensureSession；本项不动。 |
| 产出物预览主舞台（PreviewPane + TabBar + OfficeView + 舞台分栏） | 否 | 成立 | keep | 出自 AionUi（AppShell.vue:32-33 注释引用了用户参考图），是 cowork 形态的主轴。PreviewPane 在渲染端自己拼绝对路径的问题归 W8a。 | 无。 |
| 工具步骤组 StepGroup（「已执行 N 步」整组折叠） | 否 | 需修正 | keep | 保留成立：这是 AionUi 的「View Steps」（StepGroup.vue:2）。  UI 读者建议「只折叠只读类，写文件、跑命令单列」，需要修正： - 它偏离了用户选定的 AionUi 整组折叠形态； - 它依赖 W7a 的工具 readOnly 声明（roadmap.md:201），在那之前只能硬编码工具名表。 更稳的做法是保持整组折叠，在组头追加「写了 N 个文件 / 跑了 N 条命令」的摘要。  prov 的「参数收进二级详情」要调 renderer-tool-steps.test.ts:44-56 的两例（回落参数区、至少两处 slice 截断），不能让非 file_edit 工具展开后一片空白。  另发现：StageChat.vue:112 对没有结果的历史工具默认 ok:true，崩溃中断的工具会显示成功，见「引擎建议：悬空工具判失败」一条。 | StageChat 的回合切分（:86-129）属于 W2b-1、W2b-2 的改动区；W7c 已排回合折叠。W3 之后再动。 |
| 差分视图 UiDiff（权限卡写入预览 + 步骤组 file_edit） | 否 | 成立 | keep | 用自写的 LCS，不是 OpenCode 用的 @pierre/diffs。权限卡里的写前预览是安全件，换成虚拟化差分库就违反零依赖。 | 无。 |
| 权限确认卡 PermCard（对话流内联、三钮、倒计时、桥双段告知） | 否 | 成立 | keep | PermCard.vue:7-13 的三条规矩核实，对话流内渲染在 StageChat.vue:216-217。  需要调和两处表述：UI 读者说「不照搬 dock、保留流内」，roadmap W7b（roadmap.md:202）已定「停靠输入区」。以 W7b 为准，但三条规矩必须带过去。OpenCode 的 dock 只列 patterns（session-permission-dock.tsx:63-70），不显示「始终允许」会保存的范围，这一点是反面教材。  若照搬 OpenCode 的 dock 代码，需要在 NOTICES 新开 MIT 改编节。 | W2b-2 正在把 StageChat.vue:216 改成 permsHere，并给 PendingPerm 加 sessionId。发版前只做 W2b-2，不改形态。 |
| Composer 输入辅助（斜杠技能菜单、@ 文件、输入历史） | 否 | 成立 | keep | 斜杠菜单出自 OpenMinis，@ 文件和输入历史出自 AionUi。  UI 读者提议把交互抽成 lib/composer/machine.ts 纯状态机：零依赖，属于可取的 simplify，但 Composer 正在止血期被多处修改，应排在 W3 之后。「!」进 shell 模式不借。 | W2b-3 改 canSend（Composer.vue:33），W2b-4 改 :181-185、:225-234，W2a-6 加 relayDraft 消费。发版前不动。 |
| 三栏外壳 AppShell、左导航 NavRail、欢迎页 + ModelBar、会话舞台 StageChat 主体 | 否 | 成立 | keep | 出处核实：OpenMinis 复刻加 AionUi 形态（PROJECT_NOTES.md:33、:619-627），ui-audit 的三个基准里没有 OpenCode。  NavRail 目前没有运行、未读标记（NavRail.vue 里 grep 不到 running/unread）；W2b-2 加等待标，W7b 做三级徽标。OpenCode 砍掉侧栏改用 Home 加标签页（settings.tsx:60-63、:128-131），与 AionUi 形态冲突，不学。 | W2b-1、W2b-2、W2b-4、W2b-5 都在改这几个文件，保持不动。 |
| 事件提示条 EventNotes | 否 | 成立 | keep | 在对话流尾部渲染（StageChat.vue:212），是临时缓冲：open 时清空（chat.ts:430），最多 10 条。发版后接手任务面板的降级、压缩、卸载和停止原因；新增 kind 追加在 chat.ts:108 联合类型的末尾。 | W2a-1 刚加 compactFailed，W2a-6 要加 relay/short；接手停止原因排在 W3 之后。 |
| 思考块、注释层 AnnoLayer、助手与定时、扩展市场、设备、设置、会话搜索 | 否 | 成立 | keep | 全部与 OpenCode 无关：插件市场调研里 OpenCode 因为「没有机器可读的市场」被排除；设备同步的研读结论是 OpenCode 没有可借的先例。  修正 prov 一处：StageSearch 并非「没有守卫」，renderer-stage-views.test.ts:23 钉着它的挂载。 | W2b-11 要改 StageCron.vue:92 和 StageDevices.vue:87 的文案，本项不动。 |
| 引擎建议：发版前「崩溃后悬空工具判失败」 | 是 | 需修正 | simplify | 前提写错了。arch 说 DeskMinis 崩溃后界面会「一直显示运行中」，实际相反：StageChat.vue:112 对没有结果的 toolUse 默认 ok:true，界面显示成功。loop.ts:679 在整批工具跑完后才一次性写 toolResult，所以崩溃连已完成工具的结果也会丢。模型侧已经由 pairToolResults（loop.ts:258）在请求时补上「[工具执行被中断，结果未知]」。  OpenCode 的做法也不是启动扫描，而是在下一次 run() 时惰性处理（core/src/session/runner/llm.ts:397 调 failInterruptedTools）。  最小正确修法是渲染端：历史里没有结果、又不在实时回合中的工具，显示为「已中断 / 结果未知」。这属于界面撒谎一类，要进 W2b-11 须经用户拍板。逐工具即时落库放到 W6（会话快照与序号）。 | 不在 W1a–W2b 的 30 步表里；roadmap.md:311 禁止 0.3.0 前加功能；W2a 正在改 loop.ts（hemostasis/engine.md 的 files 段）。 |
| 引擎建议：发版前「达到 maxTurns 时发不带工具的收尾回合」 | 是 | 需修正 | replace | 「发版前就能做」不成立，原样照搬也不成立： - OpenCode 把 MAX_STEPS_PROMPT 作为 assistant 角色消息追加在请求末尾（runner/llm.ts:218，Message.assistant 定义在 llm/src/schema/messages.ts:210），再配 tools 为空、toolChoice:none。请求以 assistant 结尾，正是报告 §4 B#10 说的「新一代 Claude 当预填拒绝」，照搬会 400； - 它只在 agent 配了 steps 时才触发（llm.ts:202；config/plugin/agent.ts:107），默认 agent 没有步数上限。  可取的是思路：把 loop.ts:686 的 yield error「已达最大回合数」换成用 user 角色提示的收尾回合。发版后与 W5 的上下文管线一起做。 | W2a 正改 loop.ts，roadmap.md:311 禁止发版前加功能；且与 W2a 修复「请求以 assistant 结尾」的方向相反。 |
| 引擎建议：发版后第一个引擎波就做事件表 + seq 续传 + 插话/排队收件箱 | 是 | 需修正 | replace | 形状可借，已核实： - OpenCode 用 immediate 事务依次跑投影器、commit 钩子、写 sequence 和 event（core/src/event.ts:303-351）； - DeskMinis 的 messages 是可变行（store/db.ts:15-23），chat.event 广播不带 seq（index.ts:749），运行中直接拒收新消息（index.ts:602）。  时序要改：报告 §5 规定「迁移按发布合并，0.4.0 只在 W5c 做一次」。所以事件表进 W5c 那一次追加迁移，seq 续传归 W6（会话快照与序号），收件箱归 W7a，不能单独开一个「第一个引擎波」。  OpenCode 的实验库是可丢弃的（specs/v2/session.md:173），DeskMinis 有真实用户库，只能追加迁移，事件类型从第一天起带版本号。 | 与 0.3.0 无冲突（都在发版后），但与 roadmap 的迁移纪律冲突，必须并入 W5c。 |

## 架构结论核验

| 说法 | 状态 | 说明 |
|---|---|---|
| V2 还没上线：1.18.32 发行默认仍跑 V1 循环（AI SDK 的 streamText），原生 LLM 要设 OPENCODE_EXPERIMENTAL_NATIVE_LLM 才启用 | 属实·已落地 | V2 内核的代码已落地，但默认路径不走。证据： - runtime-flags.ts:54；opencode/src/session/llm.ts:226 走 native 门控，:280 调 streamText； - specs/v2/todo.md:3「work towards a launch of v2」； - 补充：app 的 detectServerProtocol（server-protocol.ts:26-35）先看 /global/health，是旧 server 就判 v1；仓内新 server 的 health.ts 只返回 {healthy:true}、不带 pid，同样判 v1。所以用仓内任一 server，app 的 V2 reducer 路径都走不到。 结论：V2 可以作「数据模型基准」，不能作「经过用户验证的行为基准」。 |
| 单写者事件表：投影和追加在同一个 immediate 事务里完成，seq 必须严格递增，重放不一致就报错 | 属实·已落地 | 证据： - event/sql.ts:4-25：event_sequence 表，event 表按 (aggregate_id, seq) 唯一； - event.ts:287-299：Replay diverged 与 Sequence mismatch 两种报错； - :320-323：先跑投影器，再调 commit(seq)； - :324-349：写 sequence 与 event； - :351：behavior immediate。 只有 V2 路径调用。better-sqlite3 的同步事务可以零依赖复刻。 |
| 持久事件与 live-only 增量分两路；sessions.events(after) 先订阅再回放；history 有限分页；全局流有损 | 属实·已落地 | 证据： - schema/src/session-event.ts:448-477 的 DurableDefinitions 不含任何 Delta；:479-512 的 Definitions 才含； - specs/v2/session.md:175-183 写明 sliding(1) 唤醒、history 默认 50、上限 100、返回 hasMore； - 全局流 server/src/handlers/event.ts:9 容量 256，core/src/event.ts:152-164 溢出时 Queue.fail(SubscriberOverflowError)。 溢出时是整条流失败，客户端能感知到断流，但不知道漏了哪些。 |
| 准入与投递分开：session_input 收件箱 + steer/queue + 客户端 id 幂等回执 | 属实·已落地 | 证据：core/src/session.ts:360-386，delivery 默认 steer；同一 id 配不同内容，经 SessionInput.equivalent 判出后抛 PromptConflictError；admit 之后调 execution.wake。 session.md:155-158 定义了两种投递语义。 规格漂移：api.html 写的 delivery 取值是 immediate/deferred，引用时以代码为准。 对应 DeskMinis W7a；ZCode 的 guide/queue 可交叉核对。 |
| 崩溃恢复：进程丢失后，悬空工具一律判失败，绝不重放 | 需修正 | OpenCode 侧属实：runner/llm.ts:119-139 发 Tool.Failed「Tool execution interrupted」。 但它是在下一次 run() 时惰性执行（:397），不是进程启动时扫描；session.md:165 也说明崩溃后续跑是刻意延期的。 DeskMinis 侧的前提「界面一直显示运行中」不成立：StageChat.vue:112 把没有结果的工具默认成 ok:true，显示为成功；loop.ts:679 整批跑完才写结果。 |
| 达到步数上限时追加一个不带工具的收尾回合（MAX_STEPS_PROMPT） | 需修正 | 代码在（runner/llm.ts:202、:218-220；max-steps.ts），但有两处限定： - 只有 agent 配了 steps 才触发（config/plugin/agent.ts:107），默认 agent 没有步数上限； - 提示以 Message.assistant 追加在请求末尾，相当于 assistant 预填，对新一代 Claude 会 400（报告 §4 B#10）。 DeskMinis 借用时必须改成 user 角色。 |
| System Context 源代数 + context epoch：基线不可变，源变化作为 durable 的 context.updated 消息追加，首次观测时有源 unavailable 就阻塞 | 属实·已落地 | 证据： - system-context/index.ts:14、:28 定义 unavailable；:82-87、:198-202 定义 InitializationBlocked； - session/context-epoch.ts:40-78：首跑 insert 基线；压缩后 replace；有变化就 publish ContextUpdated，并用 commit 钩子在同一事务里推进快照。 AGENTS.md 向上查找核实（instruction-context.ts:40-73，不越出项目根，读失败返回 unavailable）；config.ts:96-98 的 instructions 字段在 V2 里没有消费方（只有 v1/config/migrate.ts:64 拷了一下）。 对 DeskMinis 的前缀缓存（报告 §4 B#17）价值最大，对应 W5。 |
| 压缩：整请求估算 + 绝对余量；滚动合并旧摘要；溢出且尚无输出时只补救一次 | 属实·已落地 | 证据：session/compaction.ts:12-15 定义 buffer 20000、保留 8000、摘要输出 4096，还有 prior-summary 合并指令。 规格漂移：config.md:354-372 的示例值不同（keep 2000 / buffer 10000）。 估算是 util/token.ts:3-5 的 chars/4，不区分 CJK，不如 DeskMinis context-policy.ts:22 的 cjk/1.6。 滚动合并可以直接对接报告 §4 B#10 与 W5a。 |
| PermissionV2 的进步：配置 deny 先判；拒绝时同会话其余待批级联拒绝；「总是允许」后重评其余待批 | 属实·已落地 | 证据：permission.ts:200-215（deny 抛 BlockedError）；:231-248（reject 级联，只限同会话）；:250-283（always 保存规则后重评 pending）。 补充细节：always 的重评遍历全部 pending，不按会话过滤，规则按项目作用域保存。DeskMinis 的「本会话允许」只能借到本会话范围内的重评。 |
| PermissionV2 仍没修的洞：bash 把原始命令存成通配规则、默认 * * allow、拒绝附言（CorrectedError）被工具 mapError 吞掉 | 属实·已落地 | 证据： - tool/bash.ts:142-148 的 save:[input.command]；:196 用 mapError 把任何错误改写成「Unable to execute command」；permission.assert 就在被包的 gen 里，所以 CorrectedError 和 BlockedError 都会被吞； - permission.ts:209 只把 DeclinedError 转成 defect； - plugin/agent.ts:109 的默认规则是 {*,*,allow}； - util/wildcard.ts:5-13 把 * 转成 .*，Windows 上加 i 标志不区分大小写。 这是反面基准，不借。 |
| 面向 UI 的事件词表分叉：app 用 vendored client 的 session.input.* 和 session.execution.*，仓内内核只发 session.next.*，也没有执行生命周期事件 | 属实·已落地 | 证据： - app/package.json:57 依赖 file:vendor/opencode-ai-client-1.17.13-v2.tgz； - server-session-v2-reducer.ts:29-32 处理 session.input.*，:328-330 处理 session.execution.*； - schema/src/session-event.ts:448-477 的 Durable 事件只有 session.next.*，没有 execution 生命周期。 结论：DeskMinis 渲染层不应照搬 OpenCode 的事件名和 reducer；自己的封闭词表要加上执行开始/成功/失败/中断、已安排重试、压缩失败。 |
| V2 仍是半成品：compact/wait/shell/skill 不可用；V2 runner 只接 3 条 provider 路由、不降级；成本固定写 0 | 属实·已落地 | 证据： - core/src/session.ts:387-424 返回 OperationUnavailableError； - runner/model.ts 只认 @ai-sdk/openai、@ai-sdk/anthropic、带 url 的 @ai-sdk/openai-compatible 三种； - runner/llm.ts:338 的 cost 写 0； - session.md:153 说明 provider 超时和看门狗是刻意延期的。 provider、降级、重试、看门狗这几块，DeskMinis 现有实现更完整，不向 V2 倒退。 |
| Todo：整表替换，发的是非 durable 事件，也没有读取路由，所以 V2 在这块弱，改用 AionUi/ZCode 当基准 | 需修正 | 结论成立，细节要改：todo 行在 TodoTable 里按事务「先删后插」持久化（session/todo.ts:32-57），重启不会丢。弱在两点： - todo.updated 不进会话事件日志（schema/src/session-todo.ts:18-24）； - protocol 里没有读 todo 的路由（grep 零命中）。 基准改取 AionUi 的 ConversationPlanBar（含 usePlanRecovery）和 ZCode。 |
| OpenCode V2 界面是一次减法：新布局强制，旧侧栏界面在 2026-09-14 日落；文件树、终端、状态等专业件默认隐藏；权限 dock 不显示「总是允许」的保存范围 | 属实·已落地 | 证据： - app/src/context/settings.tsx:60-63 定义日落日期；:128-131 retired 后恒为 true；:183-197 的默认设置里 show* 全是 false；:279 的 visible 服从偏好；showTerminal 在 i18n 之外没有消费方； - session-header.tsx:164、:326 的 when={isV2} 传的是访问器，分支恒真； - session-permission-dock.tsx:36-70 只列 patterns； - 权限/提问的原生通知只在旧壳 pages/layout.tsx:417-455 调 platform.notify，layout-new.tsx 没有对等实现。这是静态阅读确认，未实机验证。 对 DeskMinis：取「渐进披露」原则，不取砍侧栏和「项目等于仓库」的对象模型。 |