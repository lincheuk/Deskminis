# pi:verify
总述：pi-mono（HEAD 8676a0d，2026-09-24）自基线 22ca1fffe1 以来共 540 个提交、1264 个文件变化，+169395/−50606 行。基线提交日期是 2026-08-20，当时 coding-agent 的包版本号仍是 0.84.2，但代码里已经带有一部分 0.84.3 未发布的改动，例如 session_compact_failed。

项目分两层看。

稳定面：coding-agent 的 CLI、SDK、stdio JSONL RPC，加上 pi-ai 模型层和经典 Agent 循环，这一层在持续打磨。0.84.3 到 0.87.1 之间新增的主要有：
- 可选 PowerShell 工具
- 截断的压缩摘要一律拒收
- 工具结果之后先压缩、再发请求
- 转录内系统消息（系统提示分节补丁、工具增减）
- 追加式 context_edit
- 模型图片输入限额
- /bug 诊断包与崩溃记录
- 成本感知的缓存预热
- 重试延迟上限

生产版 coding-agent 仍然用经典 Agent（core/sdk.ts）。

实验面：架构重心整体迁移了一次。
- 新包 chord（+20276 行）接手服务、订阅和复制状态的语义。
- protocol 从 v3 缩到 v8，只剩纯路由信封加 CBOR 长度前缀帧。
- 新建 durable 包（Pico5，+13357 行）。
- agent 包的耐久 harness 从 runtime2 重写为 runtime/lane 加 drive/*，并新增 pico3 内核。
- coding-agent/experimental 新增 mini、micro 和 Radius 中继。

这一层只在 PI_EXPERIMENTAL 和源码路径下使用。Unix 传输在 win32 上直接抛错；四份规范合计 8284 行，harness.md 自己写明 format 4 仍是 WIP。

对 DeskMinis 最值得吸收的，是一批零依赖的小机制：
- 识别上下文溢出后压缩并重试一次，不走降级
- OpenAI 兼容端点的 compat 矩阵（DeepSeek 思考格式、reasoning_content 回放）
- 编辑工具的稳健性：切片拼接替换、CRLF/BOM 保持；不要照搬 NFKC 模糊匹配
- 迭代式结构化压缩
- 按真实 usage 算水位，并补齐缓存读写口径
- RPC 断连时结算挂起调用、快照加序号、按订阅投递
- 运行中插话的 steer 队列
- Windows 按进程树终止、用绝对路径启动 shell
- AGENTS.md 注入前先经信任确认

核验中纠正了几处读者说法：
- DeskMinis 其实已按消息落库 token_usage，缺的是缓存读写口径，也没用它算水位。
- DeskMinis 文件工具的读、改、写之间没有 await，同一文件的并发 file_edit 不会丢失更新。
- DeskMinis 渲染端已经在入库时做了图片降采样。
- pi 的 serverId 是逻辑身份，替换进程后可以沿用，不是"进程一换就变"。
- better-sqlite3 默认已有 5 秒 busy 超时。

要点变化：
- 新包 packages/chord（28b49a6，2026-08-29，76 个文件 +20276 行）：应用组合运行时，含 facet 插件、服务令牌、单写者复制状态（单调序号，断档即清空重水合，见 src/services/state.ts:291-330）和 Delta；10d1ad6（09-21）加入事务化 COW 复制状态。服务与订阅语义从 protocol 下沉到这里。
- 新包 packages/durable（0801601，09-18，41 个文件 +13357 行）：Pico5 记录模型（conversation/entry/task/submission/document），memory、JSONL、SQLite 三种后端（STRICT 表 + json_valid；拒绝打开版本更高的库），b455975（09-23）导出存储一致性套件；规范 docs/pico-v5.md 2212 行。
- packages/agent 耐久 harness 重写（+73651/−16054 行）：删除 runtime2（−1466 行），改为 runtime/lane.ts + runtime/drive/*；新增 pico3 内核（46b66c5）、fork-policy.ts、AdaptivePublisher/OutputCapture（0095bce）。harness.md 第 158 行自述「Storage format 4 is still WIP」。生产版 coding-agent 仍用经典 Agent。
- protocol v3→v8（361 行缩到 110 行，+163/−1465）：只剩 hello/request/cancel/response/service_update/attachment 信封，严格 JSON 加 4 字节长度前缀 CBOR 帧，默认限额 16MiB。client README 写明断连时本地 reject 挂起调用、从不自动重放。
- 转录内系统消息（9e05370，#9548，0.86.0）：SystemMessage 可带 sections 补丁和 toolsAdded/toolsRemoved；只有生成目录里验证过的模型才按位置发送，其余经 collapseSystemMessages 折叠回首条。coding-agent 的 diffSystemPromptSections 把提示变更写成分节补丁。
- 0.87.0 追加式 ContextEditEntry（466db0f）：provider 上下文统一经 SessionManager.buildSessionProjection 生成；溢出恢复和重试失败的尝试通过 _omitRecoveryAttempt 从模型视图中持久省略，原始转录不动。
- 工具与压缩稳健性修正：截断摘要拒收（97fa14e）；工具结果后先压缩再请求（56700d4）；预检后中止即停止已准备的工具（afda4d6）；agent 重试延迟封顶 60 秒（c37b0e0）；Windows 改用 System32 下的 taskkill.exe；新增可选 PowerShell 工具（80e6276，08-24）；工具渲染器拆到 tools/renderers（eb3e9fe）。
- 模型层：图片输入限额与入史前一次性缩放（f5c9464，09-20）；统一 chat/image/classifier 模型类型（a328aa8，09-23）；与 pi.dev 共享的远端模型目录协议（7fd564c）；溢出识别补上 z.ai 的「Prompt too long」（0e28320），无 body 的 400/413 只对 Cerebras 算溢出（661619e）。
- coding-agent/experimental 新增 mini 三进程原型（353c990，自研 7 种帧的 RPC，带心跳、取消、断连 reject；README 把 N² 广播列为已知捷径）、Radius 云中继（1d0d110）、micro（7e19507）。0.85.0 误把实验代码发到 npm，0.85.1 用 files 排除实验目录并新增 check-runtime-deps.mjs（1382777）。
- /bug 诊断包：键名正则脱敏、URL 凭据剥离、离线模式只能导出 zip；另有 crashes.json（最多 5 条、保留 7 天），崩溃栈可归因到具体扩展（3c75b27、63787ee，0.86.0）。同版本新增成本感知的缓存预热 cache-warmer.ts（预期节省至少 0.05 美元才刷新，空闲续用概率取 0.15）。
- evals 重写（42 个文件，+3526/−1975）：Docker 隔离的成对「文档增益」评测，两臂奇偶轮交替，缺臂即阻断该组 headline，缺失的遥测记为不可用而非 0；另有文档审计 eval，用 json_schema 约束的 submit 工具提交裁决（d7296c0、42cd371）。
- session-backends/sqlite-node 原地改写 001_initial.sql，改成多会话容器，并删除 writer-lease.ts（94108a1、ef11444）。这是反面教材，因为该包未发布才可以这样做。

核验后发现（按相关度降序；verdict=CONFIRMED/CORRECTED；DM=DeskMinis 现状）：
- R5 [confirmed|providers|新:否（主机制基线已有；失败尝试持久省略、z.ai/Cerebras 修正为基线后）|DM:missing] 上下文溢出识别，压缩后只重试一次；先判溢出，再判重试或降级：pi-ai 的 overflow.ts 维护约 27 条 OVERFLOW_PATTERNS，覆盖 Anthropic、OpenAI、Gemini、Kimi、Qwen、DS4、Ollama、z.ai 等，并用 NON_OVERFLOW_PATTERNS 排除限流（rate limit、too many requests、Bedrock Throttling）。isContextOverflow 识别三种情况：error 文本命中；stop 但 input+cacheRead 超过窗口（静默溢出）；length 且 output=0、输入占满窗口的 99% 以上。isRecoverableLength 识别低于期望上限的 length 截断。coding-agent 的 _checkCompaction 只对同一模型产生的消息判溢出，分三种处理：（1）溢出或可恢复的 length：先用 _omitRecoveryAttempt 把失败尝试从模型视图中持久省略，再压缩并重试，_overflowRecoveryAttempted 防止第二次循环，再次失败就明确报错；（2）成功响应但已超窗：只压缩，不重试；（3）越过阈值：只压缩。_isRetryableError 先排除溢出。耐久 harness 的 drive/response.ts 用 overflowRecoveryUsed 实现同一语义。 ⇒ 借鉴：零依赖新建 providers/overflow.ts，正则表连同排除表一起搬，并为国内厂商补真实报错样本测试。在 ProviderError 分类里把溢出判定放在 fallbackable 之前；命中后绕开 compactCount<3 和「30 条消息」的门槛，强制调用一次 CompactEngine，然后在同一个 slot 重试，每次运行只允许一次。溢出时不降级，也不改绑会话。TDD 红测：FakeProvider 抛出 400 prompt is too long / context_length_exceeded，断言发生了 compacted、没有 fallback/rebind。（核验：合并了 agent 核心视角和 coding-agent 视角的两条同类发现。所列行号已逐一打开核对。机制在基线已存在：基线 agent-session 已有 _overflowRecoveryAttempted。基线后新增的是用 context_edit 持久省略失败尝试，以及 z.ai、Cerebras 的识别修正。）
- R5 [confirmed|providers|新:否|DM:partial] OpenAI 兼容端点 compat 矩阵：思考参数格式、reasoning_content 回放与补空、max_tokens 字段、按 URL 自动识别：OpenAICompletionsCompat 用二十多个开关描述各家兼容端点的差异，包括 supportsDeveloperRole、supportsReasoningEffort、maxTokensField、requiresToolResultName、requiresThinkingAsText、requiresReasoningContentOnAssistantMessages，以及 thinkingFormat。thinkingFormat 的取值：openai 用 reasoning_effort；deepseek 用 thinking:{type}，支持时再加 reasoning_effort；zai 用 thinking:{type}；qwen 用 enable_thinking；另有 chat-template、qwen-chat-template 等。detectCompat 按 provider 或 baseUrl 自动识别，例如 deepseek.com、api.moonshot.、api.z.ai、open.bigmodel.cn、together、nvidia。识别为 DeepSeek 时 requiresReasoningContentOnAssistantMessages=true：模型开启推理后，每条回放的 assistant 消息都带 reasoning_content，缺失就补空串。思考块按首块签名字段名（reasoning_content/reasoning 等）原样回放。model.compat 可以覆盖自动识别结果。 ⇒ 借鉴：把 OpenAICompatFlags 扩成一个小 compat 对象：thinkingFormat、requiresReasoningContent、maxTokensField、supportsDeveloperRole；加一个 detectCompat(baseUrl) 纯函数，providers.json 可覆盖。loop 落库时已有 reasoningContent，只需在 buildOpenAIBody 里按 compat 回放。用 mock-openai.mjs 分别对 deepseek、zai、qwen 三种格式做请求体快照测试。零依赖。（核验：已逐段核对。补充一个限定条件：reasoning_content 补空串只在 compat.requiresReasoningContentOnAssistantMessages 且 model.reasoning 时发生，自动识别只对 DeepSeek 打开这个开关。）
- R5 [confirmed|tools|新:否|DM:partial] 编辑工具的健壮性：切片拼接替换、CRLF/BOM 保持、多段 edits、参数垫片；模糊匹配时的 NFKC 是陷阱：pi 的 edit 工具：一次调用可带多段 edits[]，每段都针对原文匹配，并检查重叠和唯一性。读文件后先剥掉 BOM，检测行尾后统一转成 LF 再匹配，写回时还原原来的行尾和 BOM。替换用 substring 切片拼接，逆序应用以保持偏移不变。prepareEditArguments 兼容三种模型怪癖：把 edits 发成 JSON 字符串、发成单个对象、把 oldText/newText 放在顶层（注释点名 Opus 4.6、GLM-5.1）。精确匹配失败后才退回模糊匹配，而 normalizeForFuzzyMatch 的第一步就是 NFKC，并把 U+3000 转成普通空格；走模糊路径时，applyReplacementsPreservingUnchangedLines 会用归一化后的内容重写被触及的行。本地 node 验证：'，（）：　'.normalize('NFKC') 得到 ',(): '。DeskMinis 的 file_edit 只支持单段替换，用的是 content.replace(oldStr,newStr)，本地已复现 $$、$&、$' 被 JS 解释改写；没有 CRLF 归一和 BOM 处理。 ⇒ 借鉴：第一步是最小 TDD 修复：替换改成 indexOf + slice 拼接；剥掉 BOM，CRLF 转成 LF 后再匹配，写回时还原。红测三条：new_string 含 $&；CRLF 文件配 LF 的 old_string；带 BOM 的文件。第二步支持 edits 数组，并加参数垫片（数组被发成字符串、单个对象、顶层字段）。模糊匹配只做四件事：去行尾空白、统一智能引号、统一破折号、统一 NBSP；不用 NFKC，也不归一 U+3000，避免悄悄改掉中文全角标点。（核验：pi 侧和 DeskMinis 侧都已打开源码核对，并在本地用 node 复现了 $ 模式问题和 NFKC 的转换效果。）
- R5 [confirmed|context|新:否（算法基线已有；拒收截断摘要、摘要不带工具等为基线后修正）|DM:partial] 迭代式结构化压缩：只摘要增量并带上一次摘要，按 token 预算选切点，跨压缩累积文件清单，截断的摘要拒收：coding-agent 的 prepareCompaction 从上一次压缩边界之后开始取材，把上一次的摘要作为 previousSummary，放进 <previous-summary>，用 UPDATE_SUMMARIZATION_PROMPT 增量合并。结构化格式固定为 Done/In Progress/Blocked/Next Steps，并要求保留精确路径和报错。findCutPoint 从尾部累加，达到 keepRecentTokens（默认 20000）即停，不在 toolResult 处切；切在回合中间时，另对该回合的前缀单独做摘要。序列化时工具结果截到 2000 字符。读过、改过的文件清单从工具调用参数中抽取，跨多次压缩累积。getSummarizationFailure 拒收以 length 结束的摘要（97fa14e，基线后新增）；摘要请求设 cacheRetention:none，且不带工具（6b36eb5）。harness 的 compaction.ts 有同构实现，默认 reserveTokens 16384 / keepRecentTokens 20000。DeskMinis 的 compact.ts 每次从 history[0] 取材，不用已有 marker 的摘要；工具结果整段送去；maxTokens 1024，要求「不超过 500 字」的自由格式；不检查截断；loop.ts 用 catch {} 静默吞掉压缩失败。 ⇒ 借鉴：零依赖移植，分六步：（a）取材起点改为上一个 marker 锚点之后，把 marker.summary 作为 previous-summary 传入；（b）换成中文结构化模板：目标、约束、进度（完成/进行中/受阻）、关键决策、下一步；（c）序列化为 [用户]/[助手]/[工具调用]/[工具结果] 纯文本，工具结果截到 2000 字；（d）从 file_read、file_write、file_edit、office_* 的 input 中抽取路径，写入 marker 并附在摘要后；（e）finish=length 视为失败，不写 marker；（f）失败时在 EventNotes 里给出可见交代。「3 回合 / 30 条」的锚定可以换成按 token 预算选切点。（核验：合并了 agent 视角（harness compaction）和 coding-agent 视角的同一事实。两边源码已核对，DeskMinis 的 compact.ts 与 loop.ts 也已打开确认。）
- R5 [corrected|architecture|新:部分（protocol v8、mini RPC 心跳和取消为基线后；client 不重放原则基线已有）|DM:missing] 断连语义与 RPC 韧性：断连时本地拒绝挂起调用、不自动重放、取消帧、心跳判死、按调用超时：pi-client 规定：断开或释放连接时，所有挂起的请求在本地 reject；已被服务端接纳的工作仍可能在远端完成；客户端从不自动重连或重放，调用方显式调用 reconnect()，只重做已知安全的操作。mini 的对等 RPC（233 行，基线后新增）只有七种帧：call、result、error、cancel、event、announce、ping。连接关闭时 reject 全部挂起调用并 abort 全部正在执行的 handler；每 deadMs/3 发一次 ping，15 秒没有任何帧就主动断开，用来抓卡死但没断开的进程；signal 触发时发 cancel 帧，timeoutMs 按调用点可选。protocol v8 的 hello 返回逻辑 serverId；会话请求带 {serverId, sessionId, attachmentId}，服务端生成的 attachmentId 用来拒绝切换或重新附着之后的晚到帧。DeskMinis 渲染端的 rpc.ts 没有 onclose，pending 永远不会结算；main 只在端口就绪前处理 minisd 退出。 ⇒ 借鉴：不换协议，在 JSON-RPC 上补齐：① ws.onclose/onerror 时用 DisconnectedError reject 全部 pending，store 置 connection='lost'，TopBar 如实显示；② 重连后只重拉 list/get 类只读数据，绝不自动重发 chat.prompt；③ main 监听 minisd 运行期的 exit，退避重启后经 IPC 推送新的 port/token；④ 可选：用 $/cancelRequest 加每个请求 id 的 AbortController 支持取消长调用；配对远端连接加 ws ping 心跳。TDD：模拟 ws close，断言 pending 被 reject、chat.prompt 没有被重发。（核验：合并了 A10、C10、P1 三条。纠正一点：agent 视角说「进程被替换后 serverId 就会变」，这不对。server README:75 写明 serverId 是启动器给的逻辑身份，替换进程时可以复用；晚到帧靠 attachmentId 拒绝。DeskMinis 自己用每次启动的 instanceId 检测进程重启仍然合理，但那不是 pi 的语义。「不重放」原则基线已有，protocol v8 和 mini RPC 为基线后。）
- R4 [corrected|context|新:否（「编辑或压缩后旧 usage 失效」随 context_edit 在基线后加强）|DM:partial] 上下文水位：以最近一条有效响应的 provider usage 为基准，只估算其后的消息；压缩后如实显示「未知」：pi-ai 的 estimateContextTokens 从后往前找最近一条有效 assistant：不是 error 或 aborted、usage 大于 0，且时间戳不早于其前缀里最晚的消息（防止压缩摘要插入后误用旧 usage）。以它的 input+output+cacheRead+cacheWrite 为基准，只对其后的消息做 chars/4 估算。shouldCompact 的判据是 tokens 大于 contextWindow 减 reserveTokens（16384）。coding-agent 在出现 context_edit 或压缩后废弃旧 usage；压缩后、拿到新响应之前，getContextUsage 返回 tokens:null，footer 显示 '?/窗口大小'。DeskMinis 的实际情况：loop.ts:526 会把每条 assistant 的 tokenUsage 落库到 messages.token_usage，但 Anthropic 适配器只取 input_tokens，没加 cache_read_input_tokens 和 cache_creation_input_tokens；水位与压缩判定仍来自 context-policy 的纯字符估算（CJK 按 1.6，其余按 4）。 ⇒ 借鉴：改造成本很低：ContextPolicy.estimateTokens 先以最后一条有效 assistant 的 token_usage 为基准，只估算其后的消息；压缩 marker 之后的旧 usage 要排除；纯字符估算保留为兜底。同时让 Anthropic 适配器把两项缓存 token 加回 input 口径，否则开了缓存会严重偏低。压缩后、下一次回复前，任务面板显示「压缩完成，下次回复后更新」，不显示编出来的数。（核验：合并了两条发现。coding-agent 视角说 usage「既没有落库」，这不成立：DeskMinis 在 loop.ts:526 已把 tokenUsage 写进 messages.token_usage。真正的缺口是缓存口径缺失，以及水位没用上已落库的 usage。pi 侧的全部证据属实。）
- R4 [confirmed|sync-storage|新:部分（UsageEntry、MicroUsageView、evals 口径为基线后）|DM:missing] 全口径 Usage 与计价、只追加用量账本、按实际响应模型拆分成本、页脚真实用量：Usage 字段含 input、output、cacheRead、cacheWrite，以及 cacheWrite1h（Anthropic 1 小时写入）、reasoning 和 cost 明细。calculateCost 按输入量切换 tiers，1 小时缓存写入按 2 倍 input 计价。harness 另有一本只追加的 usage ledger，sqlite-node 后端有独立的 usage_ledger 表、ix_usage_seq 索引和 adjustment 列。coding-agent 的 getUsageCostBreakdown 按 provider 加 responseModel 分组，压缩、分支摘要、工具产生的用量以及 UsageEntry（基线后新增，用于缓存预热这类非对话调用）也都入账。footer 显示 R/W 缓存 token、最近一次缓存命中率、累计成本（订阅标注 sub）。micro 的 MicroUsageView 中 contextTokens 可以为 null；evals 把缺失的遥测记为不可用，而不是 0。 ⇒ 借鉴：先补齐数据口径，再做面板：TokenUsage 加 cacheRead/cacheWrite，对应 Anthropic 的 cache_read_input_tokens/cache_creation_input_tokens、OpenAI 的 prompt_tokens_details.cached_tokens、Gemini 的 cachedContentTokenCount。用追加式迁移新增 usage 表（session_id、message_id、provider、model、四项 token、cost_usd 可空、kind、adjustment），自动命名和压缩这类后台调用也各记一笔。单价未知时存 NULL，界面显示「—」。统计按降级后真正接手的模型归属。（核验：合并了 A5、C21、P8 三条。有一处小纠正：「用量与成本面板」出自交接文档 §6 的三档候选，不是 README。usage-totals 基线已有，UsageEntry 和 micro 为基线后新增。）
- R4 [confirmed|sync-storage|新:部分（Chord 复制状态、mini、transcript-provider 为基线后）|DM:partial] 快照与事件流无缝衔接：两阶段 watch、单调序号、断档即重水合、按 id 追加渲染：harness 的 lane.watch() 分两阶段：watch 时截取 LaneSnapshot，并开始缓冲这个订阅的事件；start() 再把缓冲排空。快照和事件之间不缺也不重。LaneSnapshot 包含 streamingMessage、runningTools（running 或 settled）、retry.nextAttemptAt、queues、lastResult、stats。mini 为每个展示端各建一个 watch，用 reduceLaneSnapshot 折叠事件；返回 {rebase:true} 时重新订阅。源码注释提醒：从未 start 的 watcher 会无界缓冲。Chord 的复制状态源端为每个监听者记录 hydratedSequence，副本只接受 seq+1，一旦断档就 clear 并抛错要求重订阅。渲染按 entry id 追加，只有 id 前缀分歧（压缩或导航）时才整体重绘。DeskMinis 切换会话时，先设 activeId，await chat.messages.list 之后再清空 streamingText 和 toolCards；chat.event 不带序号。在运行中的会话之间切换时，进行中的流式文本和工具卡会丢，await 期间到达的事件也会被清掉。 ⇒ 借鉴：不引入 Chord（它依赖 esbuild，API 无兼容承诺）。在 minisd 做最小版：每个会话维护 seq，以及一份进行中回合的内存快照（流式文本、运行中工具、排队项）。新增 chat.watch(sessionId)，返回 {messages, live, seq}；之后每个 chat.event 都带 seq。渲染端丢弃 seq 不大于快照的事件，发现跳号就重新 watch。纯内存，不需要迁移。先写红测：流式中途切换会话或重连，断言文本不缺不重。（核验：合并了 C18、P2，以及 A10 里 LaneSnapshot 的部分。pi 侧各文件已核对，DeskMinis 的 chat.ts:403 也已确认。harness 的两阶段 watch 基线已有；mini、Chord 复制状态和 transcript-provider 为基线后。）
- R4 [confirmed|architecture|新:否|DM:partial] 事件按附着和订阅投递，不向所有连接全量广播：pi-server 的会话请求带 {serverId, sessionId, attachmentId}。attachmentId 由服务端生成，只经带外的 attachment 消息下发，切换或重新附着后，晚到的帧因路由不匹配被拒绝。订阅更新只发给发起订阅的附着；连接断开后，要等已接纳的调用结算完才释放附着。mini README 明确把「server 向所有展示端广播，N 个端要传 N² 次」列为已知捷径。DeskMinis 的 RpcServer.broadcast 会把 chat.event 和 permission.request 发给所有连接，包括 pairing/remote 模式的配对远端。 ⇒ 借鉴：RpcServer 给每个连接维护 subscribedSessions，chat.event 和 permission.request 只投给订阅了对应会话的连接：本机渲染端默认订阅当前会话，配对远端要显式订阅。*.changed 这类失效通知可以继续广播。渲染端切换会话时用 generation 计数丢弃旧会话的晚到事件。需要先确认接力的设计意图：远端能否批准本机的权限卡；如果不能，permission.respond 要按 authMode 限制。（核验：pi 侧和 DeskMinis 侧都已打开核对；已确认 remote/pairing 连接同样进入 clients 集合。附着语义基线已有。）
- R4 [confirmed|agent-loop|新:否（clear_queue 为 0.84.4 新增）|DM:missing] 运行中插话：steer / followUp 双队列，中止时把排队内容还回输入框：Agent 暴露 steer() 和 followUp()，两个队列默认都是 one-at-a-time，也可改为 all。steer 在当前工具批次完成后、下一次请求前注入；followUp 只在本来要结束时注入并续跑。coding-agent 的 RPC 有 steer、follow_up、set_steering_mode；clear_queue 是基线后（0.84.4）新增的。interactive 模式在中止时用 restoreQueuedMessagesToEditor 把排队消息回填到编辑器。harness 把 steer/followUp 做成 lane inbox 的标签，operation_abort 事件会带回未消费的 steer 和 followUp。流式期间产生的扩展消息和用户 bash 消息会延后插入，不夹进 tool_use 和 tool_result 之间（#8537）。DeskMinis 的 Composer 在 chat.running 时直接禁止发送。 ⇒ 借鉴：minisd 为每个会话维护一个内存队列：chat.prompt 在会话运行中时入队，而不是报错。loop 在工具结果消息落库之后、下一次请求之前取出 steer 消息，作为 user 消息追加，保证 tool_result 仍紧跟 tool_use。follow-up 等本轮结束后再开新一轮。输入卡下方显示可撤回的排队芯片；点停止时把队列回填到 chat.draft，Composer 已有 takeDraft 可以复用。（核验：合并了 A6、C7。基线的 rpc-types 已有 steer、follow_up，没有 clear_queue；其余证据已核对。）
- R4 [confirmed|platform|新:部分（杀树基线已有；System32 绝对路径为基线后）|DM:partial] Windows 上按进程树终止：用 System32 下的 taskkill /F /T，并跟踪子进程：pi 的 killProcessTree 在 Windows 上 spawn %SystemRoot%\System32\taskkill.exe /F /T /PID，并用 once('error') 吞掉异步错误。基线时是按裸名 spawn('taskkill')，#6596 之后改为 taskkill 缺失也不崩溃（0.84.4）。trackDetachedChildPid 登记子进程，退出时统一杀树；bash 工具在超时和中止时都走杀树。DeskMinis 的常驻 PowerShell 在 interrupt 和 dispose 时只调用 proc.kill('SIGKILL')，终端抽屉的 dispose 也一样，在 Windows 上只会结束 powershell.exe 本身，它启动的 node、python、dev server 会变成孤儿。另外，DeskMinis 的 mcp/stdio.ts 已经用 taskkill /pid /T /F 杀 MCP 子进程树，只是按裸名启动。 ⇒ 借鉴：复用 mcp/stdio.ts 里已有的杀树逻辑，抽成一个公共函数：用绝对路径 %SystemRoot%\System32\taskkill.exe，加 windowsHide 和 error 监听，应用到 shell 的 interrupt/dispose/超时和 terminal 的 dispose。minisd 退出时统一清理。必须在 Windows 真机验证：执行 node -e "setInterval(()=>{},1000)"，点停止后在任务管理器确认没有残留的 node.exe。（核验：pi 侧和 DeskMinis 侧都已核对。补充：DeskMinis 的 MCP 路径已经会杀树，缺口只在 shell 和终端。）
- R4 [confirmed|safety|新:是（System32 taskkill 与 PowerShell 工具均为基线后）|DM:missing] Windows 可执行文件解析：用绝对路径启动 shell 和 taskkill，防止被工作区里的同名 exe 劫持：pi 的 taskkill 改为从 System32 取绝对路径，注释写明是 trusted System32 executable。PowerShell 工具经 findExecutableOnPath 用 where 解析成绝对路径后再启动，但 where 本身按裸名启动，也会搜索当前目录，所以 pi 并非完全安全。DeskMinis 的常驻 shell 和终端用裸名 spawn('powershell.exe')，cwd 设为会话工作区（可以是用户绑定的真实项目目录）；mcp/stdio.ts 用裸名 spawn('taskkill')。按 libuv 在 Windows 上的 search_path 行为，裸文件名会先在子进程的 cwd 里查找，再查 PATH。如果不可信仓库里放一个 powershell.exe，就可能被执行。 ⇒ 借鉴：改成用 `${process.env.SystemRoot}\\System32\\WindowsPowerShell\\v1.0\\powershell.exe` 绝对路径启动；如果要支持 pwsh 7，从已知安装目录解析，不用 where。taskkill 同样用绝对路径。先写红测，断言 spawn 的第一个参数是绝对路径；再到 Windows 真机，在工作区放一个假的 powershell.exe，确认修复前被执行、修复后不会。（核验：源码核对属实。DeskMinis 状态从 partial 改为 missing：三处 spawn 都是裸名，没有任何防护。libuv 先搜 cwd 的行为按其源码逻辑判断，本次没有做 Windows 实测，落地前必须真机复现。）
- R4 [confirmed|tools|新:否|DM:partial] 读取分页（offset/limit）、有界输出累积，结果末尾给出可执行的续读提示：read 工具支持 offset/limit，默认从头截断到 2000 行或 50KB。截断时在结果末尾写明「Showing lines a-b of N. Use offset=b+1 to continue.」。bash 输出用 OutputAccumulator 处理：内存里只保留滚动尾部，超限就把完整输出写进临时文件，并告诉模型路径。grep 每条匹配行截到 500 字符。DeskMinis 的 file_read 不能分页，1MB 以内整文件读入，超过则提示改用 shell；结果超过 2 万字符才卸载，桩里只给前 200 字的摘录。 ⇒ 借鉴：file_read 增加 offset/limit（行号从 1 起），默认截断并在末尾写「显示第 a-b 行，共 N 行，续读请用 offset=b+1」。offload 的桩里给出行号区间。shell 输出改为保留尾部，完整输出写入 offloads。全部零依赖。（核验：已核对 pi 的截断常量、续读提示和累积器，以及 DeskMinis 的 files.ts 和 offload.ts。）
- R4 [confirmed|context|新:是（0.87.0）|DM:partial] 追加式 context_edit：原始历史不动，只省略或替换模型可见内容：0.87.0 新增 ContextEditEntry {targetId, replacement}，replacement 为 null 表示从上下文省略，否则替换 content。它追加写入，由 buildSessionProjection 在投影时应用，原始转录、用量统计和界面历史都不变。用途包括：重试和溢出恢复时，用 _omitRecoveryAttempt 持久省略失败的半截尝试；扩展做精准修剪。出现编辑后，上下文估算会废弃此前的 usage。DeskMinis 的理念一致（raw 永不改写，推理时合成），但实现零散：offload 在落库时就替换成桩，prune 每轮在请求侧重算，compact_markers 只能整体截断。 ⇒ 借鉴：正好符合 DeskMinis「DB 只追加迁移」的纪律：新增 context_edits(message_id, part_index, replacement NULL 可空, created_at) 表，在 buildEffectiveHistory 的最后一步应用。用途：持久修剪（避免每轮重算导致请求前缀漂移、缓存失效）；降级或重试时省略失败的半截消息；为「从上下文移除此条」和「注释入上下文」打基础。同步引擎要把这张新表纳入同步。（核验：已核对。确认是基线后新增：基线文件里没有这个类型。）
- R4 [confirmed|safety|新:否|DM:missing] 项目上下文文件（AGENTS.md / CLAUDE.md）与项目信任：pi 不经信任就注入：pi 先加载全局 AGENTS.md，再从根目录到 cwd 逐级加载每一级的 AGENTS.override.md、AGENTS.md 或 CLAUDE.md，注入系统提示的 project_context 节。项目信任（trust-manager / project-trust）只覆盖 .pi 下的设置、扩展、技能等资源。docs/security.md:55 明确写着：上下文文件不论是否信任都会加载，除非关闭上下文加载。这是一个提示注入面。DeskMinis 不读 AGENTS.md 或 CLAUDE.md，但会话可以绑定真实项目目录，也有「代码助手」预设。 ⇒ 借鉴：会话绑定工作区时，读取该目录下的 AGENTS.md 或 CLAUDE.md，带路径标签放进 context 段，并设大小上限。首次加载时复用 PermCard 样式弹信任卡：显示路径和前几行，提供「仅本会话 / 始终 / 不加载」三个选项，决定按路径存进 DB。不要学 pi 在没有信任时直接注入。（核验：已核对。pi 的官方安全文档自己承认了这一点。）
- R4 [corrected|ui|新:部分（AdaptivePublisher/OutputCapture 为 0095bce 新增）|DM:missing] 工具进度流：自适应限流发布器与有界输出捕获：AdaptivePublisher（87 行）只保留最新状态：空闲后的第一帧立即发出，之后每次发布按编码字节数「购买」冷却时间，默认最小间隔 100ms、目标 100KB/s，另有尾随定时器保证最终一定发出。OutputCapture（238 行）按 head 或 tail 保留有界的行数和字节数，超出部分落到 spill 文件，并剔除控制字符。两者在 0095bce（09-01）加入 harness/utils，durable/env 里有一份副本。事件 tool_execution_update 和 onUpdate 回调在基线就已存在；经典 coding-agent 的 bash 工具在基线也已有合并节流的 onUpdate。DeskMinis 的 LoopEvent 没有工具进度事件，shell 输出要到 toolEnd 才一次性给出。 ⇒ 借鉴：加一个 LoopEvent toolProgress，由 PersistentShell 边读边推，只推 tail 视图，最终结果仍然只落库一次。约 80 行的 AdaptivePublisher 可以零依赖照抄进 minisd。GBK 兜底解码要放在 capture 之前。（核验：读者标 new_since_baseline=yes，只对了一部分：发布器和捕获器是基线后新增，事件词汇和经典 bash 的节流基线已有，已改写描述。）
- R4 [confirmed|dx-testing|新:否|DM:partial] 可脚本化 faux provider：逐次调用的响应队列、工厂函数、流速模拟：registerFauxProvider 提供 setResponses/appendResponses，按调用顺序消费脚本步骤。每一步可以是固定的 AssistantMessage，也可以是拿得到 callCount 的工厂函数。支持 tokensPerSecond/tokenSize 流速模拟、deferred 挂起轮询和 inputLimits；fauxText/fauxThinking/fauxToolCall 用来构造消息。DeskMinis 的 FakeProvider 只有 __fail__（整组一起失败）和一次性的 __tool__；交接文档 §7 第 12 条写明，降级成功那一半此前没有任何测试。 ⇒ 借鉴：给 FakeProvider 加一个按 modelId 分组的脚本队列，通过测试模式下的 RPC 或环境变量注入 JSON。步骤至少包括：第 N 次调用抛 429 或 400 prompt is too long、length 截断的工具调用、带 reasoning_content 的流、空响应。这样降级接手、溢出压缩、截断处理都能在 CI 里稳定复现。（核验：已核对。faux 的 inputLimits 是 f5c9464 顺带加的，其余基线已有。）
- R4 [corrected|sync-storage|新:是（durable 包 0801601，09-18）|DM:partial] SQLite 迁移 runner 与表规范：连续版本校验、拒绝打开更新版本的库、原子执行、STRICT 与 json_valid；反例是原地改写初始迁移：durable 的 applySqliteMigrations 先校验版本号从 1 开始连续递增，再在一个事务里建 durable_schema 单例表、读当前版本；库版本高于程序支持的版本就抛错拒绝，其余按序执行。注释写明「只追加、不改已发布」。Node 适配器用 BEGIN IMMEDIATE，出错时 ROLLBACK，拒绝异步回调；设置 WAL、synchronous=NORMAL、busy_timeout、wal_autocheckpoint，关闭时做 wal_checkpoint(TRUNCATE)。表一律 STRICT，JSON 列加 CHECK(json_valid)。反例：session-backends/sqlite-node 在基线后原地改写了 001_initial.sql，还删掉了 writer-lease.ts。DeskMinis 的 openDb 只从 user_version 循环到 MIGRATIONS.length：库版本更高时静默跳过；BEGIN 与 COMMIT 之间抛错没有 ROLLBACK；没有 synchronous 设置，关闭时也不做 checkpoint。 ⇒ 借鉴：openDb 加一个守卫：user_version 大于 MIGRATIONS.length 时抛出可读错误，并在界面提示（例如回装旧便携版去打开新库）。迁移改用 db.transaction() 包裹，确保出错回滚。加 synchronous=NORMAL，关闭时做 wal_checkpoint(TRUNCATE)。新表可以顺手用 STRICT 和 CHECK(json_valid)。红测：构造 user_version=99 的库，断言打开时抛错。以上都符合只追加迁移的纪律。（核验：合并了 A9、P16。纠正一点：读者说 DeskMinis「没有设置 busy_timeout」，但 better-sqlite3 构造时默认 timeout 就是 5000ms，这项已经有了。其余属实。）
- R4 [confirmed|product|新:是（0.86.0）|DM:partial] /bug 诊断包与崩溃记录：分级同意、密钥脱敏、离线只能导出 zip：crash-log.ts 把未捕获异常写进 crashes.json，最多 5 条、保留 7 天；下次启动提示一次；栈里匹配到已加载扩展的路径时会指出是哪个扩展（63787ee）。bug-report.ts 用 SENSITIVE_KEY 正则（api key、secret、token、password、credential、authorization、cookie）脱敏，redactUrl 剥掉 URL 凭据。同意流程：选择是否附完整会话，不附的话可以让模型写摘要，界面会明示会话将发给模型供应商。设置 PI_OFFLINE 时禁止上传，只能导出本地 zip。DeskMinis 的 diagnostics.ts 只做静态 dry-run 预检，main 和 minisd 都没有崩溃记录。 ⇒ 借鉴：main 和 minisd 各自挂 uncaughtException/unhandledRejection，并记录 utilityProcess 的退出码，追加写 userData/crashes.json（5 条、7 天封顶）。「设置→关于」加「导出诊断包」：用 office/zip.ts 打包版本、系统信息、脱敏后的 provider 配置、dryRun 结果和最近的崩溃；会话 JSON 须勾选同意才附带。不上传，不新增出网点。（核验：合并了 C20、P6。）
- R4 [confirmed|dx-testing|新:是|DM:missing] 成对行为评测：Docker 隔离、奇偶轮交替、缺臂即阻断、评分夹具对 agent 不可读：evals 重写后，plan.ts 先规划好全部 (case, variant, model, run)，奇偶轮交替两臂顺序。每臂用新的 workspace 和 HOME；harness 以 root 启动，再降权到 PI_EVAL_SANDBOX_UID/GID；Dockerfile 用 chmod go-rwx 让评测定义和评分夹具只有 root 可读。report.ts 在任一对缺失、重复或出错时阻断该组 headline 通过率，并标注 no-lift、negative-delta、saturated、flaky；缺失的遥测记为不可用。judges 用 StructuredOutputJudge 做严格结构匹配，judgeThreshold:null 表示低分是数据而不是失败。evals 仍依赖 vitest-evals，并新增了 autoevals。 ⇒ 借鉴：零依赖形态：scripts/eval-*.mjs 经 minisd RPC 驱动真实模型（opt-in 环境变量提供 key），每次运行用全新 userData 和临时工作区。只按结果态评分：工作区文件、DB 行、工具调用序列。产出 observations.jsonl，不进 npm test，也不做 CI 门禁。用于成对比较 system-prompt 或纪律块改动前后，每臂至少 3 轮。（核验：已核对。补充一点：evals 仍依赖 vitest-evals（0.15.0）并新增 autoevals；读者说的「删除」指的是旧的 summary/harness-table 报表，不是去掉这个依赖。）
- R3 [corrected|tools|新:否|DM:partial] 同一文件变更串行化（withFileMutationQueue），并行工具的预检按源顺序串行：pi：两份 withFileMutationQueue，coding-agent 61 行、harness 62 行，以 realpath 为 key 串行化同一文件的变更，不同文件仍然并行。edit 在锁内每次 await 之后检查 signal，不在 abort 监听器里提前 reject，这样锁会一直持有到正在进行的 fs 操作结束。经典循环在并行模式下，按源顺序依次做 prepareArguments、validate、beforeToolCall 预检，再并发执行；只要有一个工具声明 executionMode:'sequential'，整批就降为串行。DeskMinis：loop.ts 用 runWithConcurrency(calls,10)，权限询问在各工具 execute 内部并发发起，同一批可能同时弹出多张卡。不过 file_edit 在权限 await 之后的读、匹配、写（readFileSync→split→writeFileSync）全是同步调用，中间没有 await，Node 单线程下同一文件的两次 file_edit 不会读到旧内容、互相覆盖。file_write 和 office_write 的写入同样是同步的。 ⇒ 借鉴：DeskMinis 目前不需要照搬文件锁；如果以后把文件 I/O 改成异步，再引入按 realpath（Windows 下再转小写）做键的 Promise 链。现在的真实改进点是：loop 先按源顺序逐个完成权限判定，一次只弹一张卡，再并发执行通过的调用；file_write 在批准后重新读取，校验预览所用的旧内容没有过期。（核验：合并了 A16、C4。pi 侧机制属实。coding-agent 视角对 DeskMinis 的核心论断（同回合对同一文件发两个 file_edit 会丢失更新，且都报告成功）经源码核对不成立：读改写之间没有 await，不会交错执行，已降低相关度。仍然成立的是并发弹多张权限卡、预览可能过期这两点。）
- R3 [confirmed|architecture|新:否（代码重写，语义基线已有）|DM:partial] 耐久执行最小内核：意图与结算两段提交、工具 replay:safe|never、如实写出「结果未知」：harness 规范规定：每个外部效果（provider 请求或工具调用）先提交意图，包括预留结果 id 和工具的 replay 声明，然后执行效果，最后提交结果和后继状态。崩溃恢复只读 op state 的叶子，不回放日志。孤儿工具只有在存储和当前声明都是 safe 时才重跑；否则合成一条中断结果，内容为最新检查点加上 INTERRUPTION_MARKER：「preceding output is the latest durable progress snapshot; newer live output may be missing, and the external outcome is unknown」。孤儿 assistant 请求用已提交的帧前缀重建成 error 消息，再走正常的重试分类。AgentTool.replay 默认视为 never。DeskMinis 只在构建请求时，由 pairToolResults 补「[工具执行被中断，结果未知]」占位。 ⇒ 借鉴：只取最小子集：ToolExecutor 加 replay 声明（读类工具 safe，shell、file_write、file_edit 为 never）；执行前用 better-sqlite3 同步写一行 tool_intent，完成时在同一事务里写结果并删除 intent；minisd 启动时扫描残留 intent，never 的落成带三段式警告的正式 toolResult。不要照搬 13 叶状态机。（核验：已核对 tools.ts、recovery.ts 和 types.ts。概念在基线规范中已有，代码随 runtime2→runtime/drive 重写。）
- R3 [confirmed|sync-storage|新:否|DM:missing] 流式 assistant 帧持久化：辅助性质，结算时删除：AssistantMessageFrameEncoder 把 provider 事件编码成紧凑帧，追加到 pending.assistant_frame 列表，不逐帧等待写库。结算提交时一并删除这个列表。崩溃后用 reduceAssistantMessageFrames 重建最新的已提交部分，合成 error 消息并附上「外部结果未知」的说明。帧只是辅助：缺了也合法，也不能证明请求已经完成。DeskMinis 只在取消时用 cancelWithPartialReply 把半截文本落库；进程被杀或自动更新重启时，流中的内容会丢失。 ⇒ 借鉴：不需要帧编解码器。按每 2 秒或每 4KB 的节奏，把当前拼好的文本 upsert 到一行 draft 消息（追加迁移加 is_draft 列）。正常结束时转为正式消息；启动时发现残留 draft 就标记为中断并展示。draft 不能进同步批次，也不能当作完整回合回放给模型。（核验：已核对帧文件和恢复路径，这两者基线已存在。）
- R3 [confirmed|context|新:是（0.86.0）|DM:partial] 转录内系统消息：提示分节补丁与工具增减写进对话，保住缓存前缀：SystemMessage 支持 content（追加指令）、sections（按名替换，null 表示删除）和 toolsAdded/toolsRemoved。首条是基线，按顺序回放后续消息即得当前的提示和工具集。只有目录里验证过的模型（supportsMidConvoSystemMessages、supportsMidConvoToolAdditions，默认 false）才按位置发送，其余由 collapseSystemMessages 折叠回首条。agent-loop 的 declareToolChanges 在请求前比较工具差异，写成系统消息。coding-agent 的 buildSystemPromptSections 分为 preamble、tools、rules、docs、addendum、project_context、skills、cwd 等节，diffSystemPromptSections 生成补丁。DeskMinis 的系统提示分成 stable 和 context 两段，但记忆和技能块一变就改写整个 system，Anthropic 挂在 system 上的 cache_control 断点随之失效；会话级禁用 MCP 会改工具表，结果相同。 ⇒ 借鉴：先做「水位用真实 usage」那条，拿到 cacheRead 的实测命中率，再判断值不值得做。低成本做法：工具按名称稳定排序；会话首轮给系统提示拍快照，之后记忆和技能的变化作为带标签的增量追加在变化点。不要猜哪些端点接受中途系统消息，默认折叠。（核验：合并了 A13、C13。已确认基线没有 transcript.ts。）
- R3 [confirmed|tools|新:否|DM:partial] 截断（length）响应里的工具调用一律不执行，返回「请用完整参数重发」：stopReason 为 length 时，failToolCallsFromTruncatedMessage 让这条消息里的所有 tool call 都不执行，每个都返回 isError 结果：「hit the output token limit … Re-issue the tool call with complete arguments」。原因是流式参数用的是尽力而为的 JSON 抢救解析，可能得到能通过校验但内容残缺的参数。harness 的 drive/tools.ts 有同构的 truncatedOutcome。DeskMinis 用严格 JSON.parse，解析失败降为 '{}'，风险比 pi 低；但 stopReason==='maxTokens' 且有调用时照样执行，模型拿到的「缺少必填参数」也有误导性。 ⇒ 借鉴：stopReason==='maxTokens' 且 calls 非空时，所有调用统一合成「被截断、请重发」的错误结果，与现有的续写逻辑配合。一条单测即可覆盖。不要为了少报错改成宽松解析。（核验：已核对。基线已有 failToolCallsFromTruncatedMessage。）
- R3 [confirmed|providers|新:部分（agent 退避封顶为基线后）|DM:partial] 重试分类：配额耗尽不重试、传输中断可重试、尊重 Retry-After 并设延迟上限：isRetryableAssistantError 先排除配额和账单类错误（insufficient_quota、quota exceeded、billing、GoUsageLimitError），再匹配过载、429、5xx 和传输中断文本。provider-retry 仿官方 SDK，读 x-should-retry、retry-after-ms、retry-after 头；服务端要求的延迟超过 maxRetryDelayMs（默认 60 秒）就直接失败。基线后的 c37b0e0 给 agent 层的退避加了封顶：用 Math.min 限制在 60 秒，封顶而不是失败。DeskMinis 的 ProviderError 只按状态码分类，不解析 Retry-After，也不识别额度用尽。 ⇒ 借鉴：在 ProviderError 上解析 Retry-After，重试梯据此等待，超过 60 秒就直接降级。错误正文命中 insufficient_quota 或余额不足时，标为不可重试、立即降级，并在界面用中文写明「额度用尽」。（核验：已核对。补充区分两种上限：provider 层超限即失败，agent 层超限是封顶。）
- R3 [confirmed|providers|新:否|DM:partial] 模型元数据 thinkingLevelMap（null 表示不支持），思考档就近夹取；promptCache TTL；responseModel：Model.thinkingLevelMap 把统一的 off 到 max 各档映射成 provider 原生取值，null 表示该档不支持。getSupportedThinkingLevels 过滤出可用档位；clampThinkingLevel 在不支持时先向上、再向下就近夹取。promptCache 记录缓存 TTL，供缓存预热使用。AssistantMessage 记录 responseModel（网关实际使用的模型）和 providerThinkingLevel。DeskMinis 的 ModelCatalogEntry 只有 contextWindow、maxOutputTokens、thinking 三个字段。 ⇒ 借鉴：从 models.dev 数据里顺带解析 reasoning 档位和 cost，扩展 ModelCatalogEntry。思考档选择器按模型只显示支持的档，超出的就近夹取，避免把 'high' 原样发给不认识它的端点。（核验：已核对。）
- R3 [confirmed|ux-flow|新:部分（fork-policy.ts 与 0.85.0 修复为基线后）|DM:missing] 会话分叉、克隆、树形导航与分叉时的状态投影表：coding-agent 的会话条目用 id/parentId 组成树：/tree 可以跳到任意节点继续，离开分支时生成分支摘要；/fork 从之前某条用户消息分出新的会话文件（header 记录 parentSession），/clone 复制当前位置；RPC 提供 fork、clone、get_tree。0.85.0 修了两个坑：分叉丢失压缩边界（#8990）、在活动回合里分叉内存会话（#8937）。harness 的 fork-policy.ts（4e356c0，09-01 新增）用一张闭合投影表规定分叉时每类状态怎么处理：会话名复制；标签只在对应条目被复制时才复制；分支 tip 改写为分叉点；lane 运行态重置；op/pending/result 丢弃；遇到未知的 pi.* 保留命名空间直接抛错。DeskMinis 的候选池里有「会话分叉」，尚未实现。 ⇒ 借鉴：不要为此引入树形存储。用追加式迁移给 sessions 加 parent_session_id 和 forked_from_message_id；「从这条用户消息分叉」就是新建会话、复制该消息之前的消息，并把被分叉的那条放回草稿。写一张显式的复制表：compact marker 只在锚点位于分叉点之前时复制；注释和高亮只在其消息被复制时复制；运行态和待批权限重置；origin_device_id 重新生成。未登记的列在测试里直接报错。（核验：合并了 A19（fork-policy）和 C16（分叉 UX）。已确认 fork-policy.ts 基线不存在、fork.ts 基线已有。）
- R3 [corrected|dx-testing|新:部分|DM:missing] 存储测试装饰器（写序记录、提交门控模拟崩溃）与可导出的一致性套件：InstrumentedStorage（21 行，基线已有）记录每次 commit 的写集，用来断言写入顺序。GatingStorage（114 行，41c9795 于 08-24 新增）可以在布防后把提交挂起，逐个放行或丢弃，确定性地模拟崩溃发生在任意两次提交之间。durable 导出 registerStorageConformance（1132 行，b455975 于 09-23 新增），任何后端都能跑同一套用例。telemetry 和 server 也各自导出与测试运行器无关的 testing 子路径。DeskMinis 的四个 provider 各自写测试，覆盖的语义不一致，也没有 ChatStore 故障注入装饰器。 ⇒ 借鉴：两个方向：① ChatStore 测试装饰器，让第 N 次写入抛错，然后断言重启后历史仍然合法：没有空 assistant、没有孤儿 tool_use 让会话报废；② 抽一张 providerConformance(fixture) 用例表，覆盖流式文本、工具调用、usage 归一、中途 abort、429/401/5xx 分类，四个适配器对本地假端点跑同一套。零依赖。（核验：合并了 A20、P12。纠正新旧：instrumented-storage 基线已有，gating-storage 和 durable 一致性套件为基线后。DeskMinis 状态从 unknown 改为 missing：交接文档与测试目录里都没有同类装饰器。）
- R3 [confirmed|tools|新:部分（渲染器拆分为基线后）|DM:partial] 工具结果分 content（给模型）与 details（给界面），工具自带提示片段，渲染器单独注册：ToolDefinition 有 promptSnippet 和 promptGuidelines（只有激活的工具才拼进系统提示的规则部分）、prepareArguments、executionMode、renderCall/renderResult。执行结果中 content 发给模型；details 持久化但不发给模型，例如 edit 的 diff 和首个改动行，bash 的截断信息和完整输出路径。基线后（eb3e9fe，08-26）渲染器拆到 tools/renderers。DeskMinis 的 ToolOutcome 只有 {output, success}，渲染端要从 input 里用 extractEditPair 加 LCS 重新算一遍 diff。 ⇒ 借鉴：给 ToolOutcome 加可选的 details（JSON，落库但不进 provider 请求）：file_edit 返回真实 diff 和首改行号，shell 返回截断信息和 offload 路径。StepGroup 按工具名查组件表，没有 details 时回落到参数区。MCP 被禁用时，它的准则随之从系统提示中消失。（核验：已核对。）
- R3 [confirmed|ux-flow|新:否|DM:partial] 权限预览基于真实文件计算（带行号和上下文），注定失败的编辑先报错、不打扰用户：pi 的 edit 渲染器在参数流式到达时，就用 computeEditsDiff 按执行时的同一套逻辑计算预览，匹配失败直接显示错误。generateDiffString 默认带前后各 4 行上下文，并给出 firstChangedLine。DeskMinis 的 file_edit 先经 guardWrite 弹权限卡，预览只拿 old_string 和 new_string，没有行号和上下文；匹配（未找到或不唯一）要等批准之后才做，一个注定失败的编辑也会先请用户批准。 ⇒ 借鉴：在 guardWrite 之前，先在内存里做一次和执行时完全相同的匹配（CRLF 归一之后）：匹配失败就直接把错误回给模型，不弹卡；匹配成功就把带行号和前后 4 行上下文的 hunk 放进 PermPreview。复用 UiDiff 和 lib/diff，不需要新依赖。（核验：已核对 DeskMinis 里 guardWrite 与匹配的先后顺序。）
- R3 [confirmed|ux-flow|新:否|DM:missing] 手动压缩（可附焦点指令）与 handoff 接力到新会话：pi 支持 /compact [instructions]，指令以 Additional focus 拼进摘要提示；RPC 和 micro 的 controller 也有 compact(instructions)。示例扩展 handoff 让模型把当前会话提炼成新会话的首条提示，放进新会话的编辑器，由用户审阅后再发送。DeskMinis 的 chat.* RPC 里没有 compact。 ⇒ 借鉴：任务面板加「立即压缩」按钮，可选填一句焦点说明，对应新增 chat.compact RPC。再加「接力到新会话」：生成一段自包含的提示，新建会话，写进 chat.draft 等用户确认。（核验：已核对 DeskMinis 的 RPC 方法全集。）
- R3 [confirmed|extensibility|新:是（fail-closed 与可续跑边界事件）|DM:partial] 生命周期钩子：可阻断、可改写，fail-closed，以及「要求再请求一次」的边界事件：扩展事件大致分四类：可改写的 context、before_provider_request；可阻断或改参数的 tool_call；可改结果的 tool_result；可取消或自带摘要的 session_before_compact。emitToolCall 只要有一个 handler 返回 block 就立即短路。基线后新增：user_bash 的 handler 出错即中止，不回落到本地执行（fail-closed）；turn_end 和 agent_before_settle 可以追加条目并要求再请求一次（0.87.0）；context_with_system。DeskMinis 的权限、offload、prune、记忆注入都内联在 594 行的 loop.ts 里。 ⇒ 借鉴：不开放第三方代码扩展，但可以在 minisd 内部建一张类型化的 hook 表，把权限、offload、prune、记忆注入拆成独立 handler，各自单测。安全类 hook 一律 fail-closed。「收尾前检查」可以用来校验定时任务是否真的完成。（核验：已核对。）
- R3 [confirmed|dx-testing|新:是|DM:missing] 文档与实现一致性审计 eval：逐页检查，只认运行路径证据，结构化裁决：documentation-audit.eval.ts 给 docs 下每个 .md 生成一个用例，agent 只有只读工具，外加一个 terminate:true 的 submit_documentation_audit 工具。该工具用 constrainedSampling json_schema（strict: prefer），裁决取 match、mismatch 或 inconclusive。提示词要求：只有明确的、用户可见的矛盾才判 mismatch；名称、注释、类型、孤立 helper、测试都不算证据；证据不足就判 inconclusive；mismatch 必须引用原文和实现符号。 ⇒ 借鉴：对应交接文档 §7 里「界面撒谎」和「全绿不等于界面正常」两条教训：把 README 能力表的每一行、设置页文案、权限卡说明做成用例，发版前手动跑，产出 mismatch 清单交人工复核，不做 CI 门禁。（核验：已核对。相关度从 4 下调到 3：这是开发流程工具，不是产品能力。）
- R3 [corrected|architecture|新:部分|DM:missing] 进程拓扑与生命周期：每会话一个 worker、按展示需求回收、空闲退役、替换后续跑持久操作：实验 server 通过 SessionWorkerManager 为每个会话 spawn 一个 worker，启动、关停、发现、需求更新的超时分别是 15s、10s、5s、5s，并把附着转换成「展示需求」下发给 worker。这部分和 coordinator 在基线已经存在，基线后被重构（+110/−147）。基线后新增的是：mini 的 server 在最后一个展示端离开 10 秒后退役；worker 被替换后，对未完成的持久操作调用 lane.resume() 续跑。 ⇒ 借鉴：不照搬每会话一进程（Windows 进程成本高，也会有多个 better-sqlite3 写者）。可以借：① main 监督 minisd 并退避重启；② 运行中的回合在 DB 标记 running，启动时扫描，界面提示「上次中断」，由用户决定是否继续，不要自动续跑；③ MCP 子进程和终端按需求计数回收。（核验：读者标 new_since_baseline=yes，只对了一部分：per-session worker、demand 机制和超时常量基线已存在，只有 mini 的空闲退役和 resume 是新增。）
- R3 [confirmed|dx-testing|新:是|DM:partial] 实验代码与发布隔离，外加依赖与入口图守卫脚本：0.85.0 把实验性的 client/server 代码和依赖误发到 npm，导致 SDK 导入失败。0.85.1（09-05）在 package.json 的 files 里排除 dist/client、dist/experimental、dist/cli/experimental，同时新增 check-runtime-deps.mjs：用 TypeScript AST 扫描每个 import，检查它是否在运行时依赖里声明。check-entry-graphs.mjs（5507d76，08-26，比事故更早）把「入口点是成本契约」写成预算守卫，注释举例说一处 export * 能让窄入口拖进约 37MB 的模块图。两者都挂在根 package.json 的 check 脚本上。 ⇒ 借鉴：把「零新 npm 依赖」变成会先红的测试：冻结 dependencies/devDependencies 快照做比较。再加源码守卫：src/minisd 不得 import electron 或渲染端模块，src/renderer 不得 import node: 内置模块。用正则扫描即可，不需要新依赖。（核验：已核对。小纠正：check-entry-graphs 早于 0.85.0 事故，不是同期加的。DeskMinis 有大量源码守卫，但没有依赖冻结守卫（tests 里只有 auto-update.test.ts 断言 electron-updater 存在）。）
- R3 [confirmed|ui|新:是|DM:partial] 表现层边界：视图只拿只读 View 和窄 Controller，错误折进视图：micro 虽然是单进程，边界仍按 RPC 的形状设计。TUI 只能拿到 MicroView（会话、对话、模型、用量、鉴权提示、notices、fatal）和 MicroController（prompt、steer、followUp、compact、abort、setModel、login 等）；harness、存储和凭据都不越过这条边界，命令失败折进 notices。实验 client 同样只经 AgentController 驱动，只渲染 Transcript 的复制值。 ⇒ 借鉴：渐进收敛：把 StageChat 需要的状态收成一个只读的 SessionView 计算对象，加一个窄的 SessionController，错误作为 notices 数组放进视图，不再散在 lastError、retryNote 等字段里。不要一次性重写 chat.ts store（它被 20 多个测试断言）。（核验：已核对 api.ts。）
- R2 [corrected|providers|新:是|DM:partial] 模型图片输入限额与入史前一次性缩放：模型元数据增加 inputLimits：maxRequestBytes，以及 images 下的 resize{maxWidth,maxHeight,maxBytes,jpegQuality}、maxPerMessage、maxPerRequest；generate-models 按 provider 填默认值。coding-agent 在图片进入会话历史之前，按目标模型的 resize 配置缩放一次，之后每次请求的字节都相同，对前缀缓存友好。DeskMinis 并不是「全仓没有 resize」：渲染端的 lib/attach/downsample.ts 在入库前按长边 1568 缩放，并设 5MB 上限，Composer.vue:192 调用它；请求侧还有 placeholderOldMediaRefs，只保留最近几轮的图片。缺的只是按模型或 provider 区分的限额，以及每请求图片数量上限。 ⇒ 借鉴：DeskMinis 已经在入库时做了一次性缩放，前缀缓存稳定。可以补的只有：按 provider 设每请求图片数量上限（例如 Bedrock 每条消息 20 张），以及单请求字节上限的兜底检查。（核验：pi 侧属实。读者说 DeskMinis「全仓没有 resize」，这不成立：渲染端 downsample.ts 已经实现，并接进了 Composer。据此把相关度从 3 降到 2，状态改为 partial。）
- R2 [confirmed|platform|新:否|DM:missing] 遥测契约：显式传递的回调式 span、类型化 schema、一致性套件；产品内只有可关闭的安装计数：pi-telemetry 零依赖，不用 AsyncLocalStorage；startSpan 是回调式，记录方法同步、永不抛错；defineTelemetrySchema 声明属性及其敏感度元数据。harness/telemetry.ts 定义 pi.ai.request 的 usage、首字节时间等属性。coding-agent 层的「遥测」只有 PI_TELEMETRY 或设置可以关闭的安装计数，外加 provider 归属请求头。自基线以来，telemetry 包只改了 CHANGELOG 和版本号。 ⇒ 借鉴：DeskMinis 不外发遥测。可以写一个约 80 行的内存 span 环形缓冲，记录 TTFT、重试和降级、工具耗时、压缩次数，供任务面板和诊断包使用，只记标量。（核验：已核对。相关度从 3 下调到 2：包本身基线后没有实质变化，可借鉴的只是约定。）
- R2 [confirmed|platform|新:是|DM:partial] Radius 云中继：主机主动外连、帧头多路复用、指数退避、显式状态枚举：实验 server 主动外连到 Radius 网关，用子协议 pi-session-relay.host.v1 表明主机身份。18 字节帧头含版本、类型和 16 字节 connectionId，在一条 WS 上复用多个客户端连接。重连退避从 1 秒到 30 秒，每次连接都重新解析凭据。主机状态是显式枚举：not_authenticated、connecting、connected，以及带 error 原文的 retrying。 ⇒ 借鉴：中继本身不借鉴。DeskMinis 的 sync/outbound-client 可以借三点：退避封顶 30 秒、成功后重置；每次重连重新读取凭据；用带错误原文的显式状态枚举驱动 TopBar 的 syncDot。（核验：已核对。相关度从 3 下调到 2：可借鉴的只是很小的约定。）
- R2 [confirmed|safety|新:否|DM:partial] 效果闸门：检查与发起在同一个同步表达式里完成：Gate.admit(invoke) 同步检查状态（aborting 或 closed 时抛错），检查与调用之间没有 await，所以中止先到时效果永远不会启动。规范强调不能把准备过程包进 admit。调用方的 Context 取消只影响它自己的观察，持久化中止只能经 requestAbort 提交。DeskMinis 在 execute 开头和权限等待之后都检查 signal.aborted。 ⇒ 借鉴：可以把「最后一次检查到 spawn、写文件或 fetch」收敛成一个 admit(signal, fn) 小工具，并为「先中止」和「先准入」两种时序各写测试。（核验：抽查属实。文件实际是 64 行，不是读者写的 1-83。）
- R2 [corrected|providers|新:是|DM:missing] 统一类型化模型目录（chat/image/classifier）与版本化远端目录协议：ModelType 分为 chat、image、classifier，分别走 stream、generateImages、classify。provider 失败或中止时返回带 stopReason:error/aborted 的结果对象，但如果用错了模型类型，会直接 throw ModelsError，并不是「永不 reject」。远端目录的修订号是 sha256，index 按 minimumPiVersion 选择兼容修订；models-store 保存 ETag，用 If-None-Match 做条件请求。 ⇒ 借鉴：图片生成在 DeskMinis 属于需要先出设计稿的三档，不急。可以顺手借两点：model-catalog 保存 ETag、改用条件请求；非聊天能力的调用约定返回带错误的结果对象。（核验：纠正：「两者都永不 reject」不准确，类型不匹配时会抛出。其余属实。）
- R2 [confirmed|multi-agent|新:是|DM:missing] Pico5 耐久记录模型：会话所有权树、任务依赖、提交去重：ConversationRecord 带 parent（分叉来源和截止条目）与 owner（创建它的任务）。TaskRecord 带 after 依赖、background、abortRequested 和先写者胜出的 memos；无法迁移的任务变为 orphaned。提交按 requestId 在会话内去重（submissionByRequest）。 ⇒ 借鉴：DeskMinis 没有子代理，整体偏重。可以单独借一点：chat.prompt 带客户端生成的 requestId 做去重，配合断线重连，避免重复发送。（核验：抽查属实。）
- R2 [corrected|providers|新:否|DM:partial] 在 provider 边界统一清理孤立代理字符（lone surrogate）：sanitizeSurrogates 删除没有配对的代理字符，保留正常 emoji，在 provider 序列化请求体时调用。DeskMinis 的 offload 摘录已按码点截断。读者举的 files.ts 里的 previewClamp 只用于权限卡预览，不会进入 provider 请求，所以不是这个风险的实际证据。 ⇒ 借鉴：作为最后一道兜底，在三个 provider 的请求体构建处统一过一遍单行正则清理，成本极低。（核验：纠正 DeskMinis 侧证据：previewClamp 截出的文本只进 UI 和权限广播，不进模型请求，风险点需要另找（例如工具输出的截断路径）。）
- R2 [confirmed|ui|新:否|DM:partial] 通用的「向用户提问」请求协议与 question 工具：RPC 模式定义 extension_ui_request，方法有 select、confirm、input、editor、notify；select、confirm、input 可带 timeout，editor 不带。示例扩展 question 实现了由模型发起的选择题。ui_prompt_start/end 事件（0.84.4）用来区分「在等用户回答」和「真的在干活」。DeskMinis 目前只有权限卡这一种向用户要输入的方式。 ⇒ 借鉴：把权限卡泛化成通用请求（选项、确认、文本），带超时和默认值，沿用「90 秒自动拒绝」的交代；加 ask_user 工具；任务面板区分「等待你回答」和「运行中」。（核验：抽查属实。）
- R2 [confirmed|agent-loop|新:否|DM:missing] 用户手动执行的命令结果进入上下文，流式期间延后插入：用户用 ! 执行的命令记成 bashExecution 进入模型上下文；!! 表示 excludeFromContext，只记录不送模型。agent 正在流式输出时延后插入，不夹进 tool_use 和 tool_result 之间。 ⇒ 借鉴：终端抽屉加一个「把最近一条命令及输出发给助手」按钮；agent 运行中就排队到本回合结束后再追加。（核验：抽查属实。）
- R2 [confirmed|providers|新:是|DM:missing] 成本感知的 prompt 缓存预热（不建议照搬）：cache-warmer.ts 在缓存 TTL 的 90%（至少留 10 秒余量）时发一次刷新请求，预期节省至少 0.05 美元才发；空闲期用户会继续的概率取常数 0.15。 ⇒ 借鉴：桌面应用在后台自动发请求，等于在用户不知情时花钱，也违背「不驻留后台」的理念，不要默认开启。可以借的一个观察：等 90 秒审批这类长时间工具运行，可能让 5 分钟缓存过期。（核验：已确认基线没有这个文件，常量属实。）
- R2 [confirmed|extensibility|新:是|DM:missing] Chord：facet 插件、同步 setup 推导依赖图、热重载、先校验完整性再加载：setup 必须同步，只能声明 provide/use/observe，宿主据此推导依赖图。服务形状不变时可以热替换。bundle-loader 先校验 sha256，再用 node:vm compileFunction 编译，不进 Node 模块缓存。chord 的 package.json dependencies 只有 esbuild 0.28.2。 ⇒ 借鉴：DeskMinis 没有 JS 插件，不需要。如果将来开放，可以借三点：同步声明加依赖图校验、加载前校验完整性哈希、永不执行 npm 生命周期脚本。（核验：抽查属实。）
- R2 [confirmed|safety|新:是|DM:partial] protocol v8：严格 JSON 边界、帧限额、Unix socket 权限隔离，不做对端鉴权：所有 schema 拒绝未知字段，载荷递归拒绝 NaN/Infinity、undefined、字节数组和循环引用。默认限额为 16MiB 单帧、100 万元素、64 层嵌套。Unix 传输的 socket 权限为 0600、目录为 0700，另有 maxPendingBytes 背压上限。README 写明对端鉴权不由实验传输实现。 ⇒ 借鉴：对 pairing/remote 入站参数做结构校验，给 ws 设 maxPayload。回环 WS 任意网页都能连，token、回环、Origin 三道门都要保留，不能学 pi 只靠文件权限。（核验：抽查属实。）
- R2 [confirmed|platform|新:否|DM:partial] 稳定集成面 stdio JSONL RPC：严格按 LF 分帧，区分 agent_end 与 agent_settled：rpc.md 要求严格按 LF 切记录，不能用 Node readline，因为它会把 JSON 字符串里合法的 U+2028/U+2029 当成行分隔。agent_end 之后仍可能有重试、溢出恢复、压缩、follow-up；只有 agent_settled 才表示不会再自动继续。agent_settled 基线已有；clear_queue 为基线后新增。 ⇒ 借鉴：minisd 在降级、重试、压缩全部结束后广播 chat.settled，e2e 剧本等这个事件，不再轮询 running（对应交接文档 §7 第 11 条的 waitIdle 假阳性）。（核验：抽查属实。）
- R2 [corrected|ui|新:部分|DM:partial] tui-plan 布局约束与全屏转录的滚动交互：tui-plan.md（2026-08-15，基线后没有改动）定义了 ScrollView 的 follow:end、滚动链，以及键盘焦点与滚轮目标分离。基线后的交互更新：可点击的「Jump to latest message」（0.85.0，#9080）；滚动条在指针进入时才显示并支持点击轨道跳转（0.84.4）；按住 Alt 滚轮 5 倍速（0.85.1）。mini 渲染时按 entry id 追加，只有前缀分歧才整体重绘。 ⇒ 借鉴：StageChat 在 following=false 时，在输入卡上方显示可点击的「回到最新」；消息列表用稳定的 :key 按 id 追加，压缩或切换会话时才重建。（核验：纠正版本号：读者写成「Jump to latest 在 0.84.4、滚动条在 0.86.0」，按 CHANGELOG 分节实际是 Jump to latest 在 0.85.0、滚动条在 0.84.4。）

未覆盖：
- packages/agent/src/harness/pico3（46b66c5 任务内核）以及 durable 的 Pico5 实现细节（kinds、调度器、文档作用域版本化），只读了类型和文件存在性
- harness.md 中标为未实现或有矛盾的条目（J1、C1、R11/R12、T1、S3、H1）和 post-wp05-roadmap.md，没有逐条核对
- packages/chord 内部：delta/astra、cow、tracker，事务化复制状态（10d1ad6）的具体语义，以及服务 wire 语法和错误码
- packages/tui：原生剪贴板 C 助手（X11/Windows/macOS prebuilds）、LaTeX 渲染、全屏转录搜索的实现，与 tui-plan 的对照
- coding-agent 的交互细节：/model 与 /thinking 只作用于本会话、Ctrl+S 保存为默认（#5263）的代码路径；设置文件无效时的警告（#7829）；会话列表渐进加载；摘要条目默认折叠
- 受管安装的分阶段校验与原子激活、jiti 按需加载、git 包 peer 依赖处理（#9863）、项目信任的完整流程
- 内置工具默认启用 strict-prefer JSON schema 约束采样（0.86.0）对各 provider 的兼容处理
- packages/ai 各 provider 文件：Bedrock/Vercel 的 1 小时缓存计价修正、Meta provider、typesafe 分类 provider、Gemini 与 Anthropic 的缓存断点策略
- coding-agent/experimental 的 facet 插件包构建与热重载、session-worker 的插件代际，以及 client-tui 桥接
- session-backends/sqlite-node 存储格式 4 的完整表结构与触发器（只看了 usage_ledger）
- evals 的 TUI footer eval（42cd371）、acme-server 的具体评分逻辑，以及 docker 双镜像构建细节
- 与 oh-my-pi 分支（基线报告 §1.7 的来源）的差异对照，本轮没有涉及